Decision 40: predicted deaths were jarring, so the client now kills only its own hull (sim_settings deathless+mortal_ship) and remote death fx wait for snapshot confirmation. Also fixed a bot-departure camping deadlock. CI status needs the GitHub MCP tools; curl 403s.                   
Safari WebTransport needs datagrams.createWritable() (Chrome/Firefox only the deprecated .writable); client takes either now. Reroll 429s after 30/hour per IP and the confirm card now shows why. Clanker Constitution vendored at root, AGENTS.md points at it, CLAUDE.md symlinks it.       
Gunners shipped: sim carrier byte + sim_attach, C2S_ATTACH=10, drones on a per-hull ring, ATTACH/DROP on the player card, d key to drop off, your own drone haloed. docs/design/gunners.md, drones_test.lua.                                                                                   
Landing lore plus the mines feature: push branch:main because local main is a stale clone, take main's OptMem store on conflict and re-note; mines are charge slot 2 with 'still' and blast_up, repel converts one to a bomb, and pack.c must learn every new spec field.                      
Facet's second barrel kills multifire on it (54.9% to 34.6%, z=5.78), kill payouts float off the wreck, mines live 12000 ticks by our choice. mapgen redrawn for a 3-tile hull with centred shapes; alpha ships seed 28, drill-picked.                                                         
Maps need drill A/Bs not density checks (kills/bot-min swings 0.60-0.98); VIE WarpRadiusLimit=20 tightens spawns, alpha runs spawn_radius=18. Admin panel: accounts.admin + /v1/admin/*, deploy/admin/ page, VW_ADMIN_HOST guard, grant curl-on-host. CCR env has VULTR_API_KEY.               
Admin panel live (LE cert+CSP); VW_ADMIN_TOKEN retired, accounts.admin set only by operator SQL since neighbour containers can call loopback routes but not the DB. Hull balance is hitbox area r=-0.91, one flight struct. Execute runbook SQL before shipping; apt postgres works.           
Death cards gone; arena/guide.lua teaches by trigger, asks retiring on the doing and tells on sim events. EV_WARP is two events and EV_PRIZE v is +1/-1. American spelling is house style. origin/main gets force-pushed to unrelated lineages, so check merge-base before landing.            
Multifire has a touch cell; panel dashboard relays the fleet over loopback. Spawn radius is sized by seconds of bullet flight, not radar reach (Alpha 250). Alpha hull matrix at 0/30/60 greens: spread 19.2/10.9/10.9, hitbox r -0.9 to -0.5, spent by 30; Wedge last throughout.             
Ranking: idempotent ingest, tiers in the pilot panel, AI gains capped. No carrier credit share. Admin panel: operator verbs via /v1/admin/command (actor from the DB), pin travels in Status, no automatic drain; pilot edits are reroll (dealt only) and a note.                              
Admin panel: typed renames, panel-granted admin flags, a live-filtering pilot table, fleet rows with rooms/build/console link. Three of server.md's five metrics were never measured. meta serve() maps a fixed status list; the page deploys before the binary and tolerates old shapes       
The trigger help system, hover label and death cards are all removed at Chris request; ui.row_at stays for stack_test and the menu keyboard page stays. A zone spawn_radius leaks into all three calibrate harnesses and is held at 0 there.                                                   
arena/controls.lua is the one list of controls, drawn under H and as touch rows in the menu. Pilot event log (decision 42): per-connection session on Seat, generic Spool<T>/Spools, pilot.jsonl to /v1/pilot-events, no IP by design, read at /v1/admin/events on the pilot card.             
Tiers are Newb/Wing/Lead/Ace/Legend. Pilot log gained died/kill rows and a SIGTERM leave handler; Alpha is FFA so team events never fire. Pilot table shows rank/rating/tier. Cipher topped all six maps on geometry alone (12px wide vs 20-34, no cloak); now 1300, spread 23.6 to 8.8.       
Ops: arm arena spools at n%3000==1 so restart joins work; meta::call supports HTTPS via rustls and VW_META_CA. Admin has four routed views, stats, 25-row paging; events use limit+1. Hero uses three 4.3s windows in one 13.8s 960x540 WebM, 766 KB.                                          
Homepage founder letter follows hero with Chris's exact copy, no heading/card, cyan rule, Discord link, mobile editorial width. 4v4 respawn 400->1200 lowers deaths ~10%, not balance; hull tuning done. Site mark is aligned colored rows, 18x22 with 9px gap.                                
Vectorwake's aligned 2x3 wedge mark is shared across public, admin, play, and game: heavy favicon cut, ordinary install cut, 0.6-unit web-header outline, and solid flat-ended game wakes. Game rows animate together. Assets and prior CI/live renders were verified.                         
Alpha spawn radius 60 jitters around side spawn (~3s bullet flight at 51); center if none. Bots use /v1/session, restoring accounts/rated careers. Never hand-merge OptMem: take main and re-note. Founder CTA 'Start playing now!' links to play.vectorwake.net; Discord for feedback.        
Ghost-room fix: net.lua reports a dead server rather than redialling; wt.rs closes its endpoint on SIGTERM. Discord is a rail stop opening the /discord redirect, wearing the official mark traced and baked since brand rules forbid recoloring. The rail overflowed a phone at 7 stops       
Public site: multiplayer hero; founder letter; Features; GitHub/Discord new-tab links; /pilots game-only stats with durable human/bot careers. Caddy route changes bump vw.site. Rail Discord mark uses traced path, faint fill/stop-color outline, and hole-aware triangulation.              
Public copy says "free and source available" (PolyForm is not OSI open source), and /pilots opens on search plus table. Opening a tab from Lua is blocked on every phone, so client links are real DOM anchors the page lays over the canvas via window.vwLink fed by ui.link_dom.             
Mines became the bomb trigger's other posture rather than a charge: shift+tab or a touch cell, no inventory, limited only by mine_max live mines per hull.                                                                                                                                     
Designed key rebinding (chip grid, conflicts swap; not built). SVS mines: MaxMines=5, TeamMaxMines=12, SeeMines Weasel only. Client browser errors grouped in the admin panel, 30-day expiry, no IPs. Identity rebinds by rejoin on mid-game login/reroll/logout-guest; costs the ship.        
Key rebinding shipped (binds.lua, conflicts swap, esc unbindable); star flicker was vertex truncation, world layers now sized via world.world_budget. Launch plan: legal sign-off, capacity, ping role, then Subspace crowd + Show HN. Homepage features are Chris's dictated 8 cards.         
LuaJIT caps 60 upvalues/function; arena.script hit it and CI lint missed it, luac5.1 -p now guards every client file. Controls board binds on key click, esc/caps/ctrl/enter/bksp fixed. Site Discord button wears the official mark tinted currentColor, Chris's call.                        
Snapshots became interest-culled (C2S_VIEW, maphack closed, proven live) and key bindings became chords, both landing the same day.                                                                                                                                                            
image.yml starves: 3.2 min builds, cancel-in-progress kills overtaken runs, no retry, so check which build is live before measuring prod. pilot predict_err was doubly wrong (real 0.04 px worst, 0.01 mean). sim_pack_around culled a pilot's own mines; a viewer's rounds now skip it.       
Client review found 24 bugs. Fixes include atomic snapshot decode, spectator guards, account/touch/SFX/terrain/join/snapshot state, plus save/drift tests; 31 Lua tests pass. Logo is unified and logo_test pins flat paths and widths across site/admin/play/game.                            
Client pass: 38 Lua suites, syntax and sound checks pass. seg_flat and single_file.py logo now match antialiased flat ui.lua mark, pinned by tests. Chris requested a hostile-system review of zone state/protocol/ratings, account/admin auth, bots, and deploy/recovery.                     
Review found rating-reset, public endpoint DoS, admin ban/lockout, spool, bot-account, bootstrap-secret, split-deploy, and restore gaps. Bomb blasts stay predicted; deaths are server-confirmed and measured by DEATH?. Desktop first-zone help prompt is approved for main.                  
2026-08-13: Meridian was the retired eighth ship, like SubSpace Shark; stale class-7 bot data referred to it. Network fixed pre-welcome false loss, but fly/watch lacks an epoch; one-shot WELCOME, snapshot gating, heuristic rewinds, and stale packet crossover can desync state.           
Moved the whole fleet from Atlanta to Dallas and tore Atlanta down, learning on the way that fleet.sh could not see an apex DNS record because Vultr omits its name key.                                                                                                                       
Chased down false lag restrictions: fresh joins seed idle ticks, misses use an exact 50-tick window, and the server's receipt window is now 128 bits.                                                                                                                                          
Alpha favicons use the clipped mark; icon.svg stays opaque for Discord/PWA. Lag policy keys only on server-observed input silence (250ms weapons, 1s controls, 15s spectate), excludes bots from miss telemetry, and sends roster-valid attach asks even when targets are filtered.            
Mobile controls: reverse a held pad, multifire an upward gun pull, mine on bombs, charges fixed; live mobile shots need hasTouch/isMobile run from /tmp. Greens can land inside walls (open doors, plus an unfound path); update_prizes sweeps them off non-ground now.                        
Two wire fixes (tile_floor for UB, UNI_INFLIGHT 16 for the stall) plus per-hull spawn seeding, since shared-rng draws for unseen hulls desync a filtered client. Live: 241 human accounts, 67% never scored, median 3 games, 2 past 100. Arrivals fine, first session is the wall.             
Two point-vs-box bugs fixed (door crush warp, greens in walls), then HUD hover cards for the corner stack, landed on main. The flight HUD is DejaVuSansMono at 26 and glyph_w rides its 1233/2048 advance. Prod alpha has no bots: meta is 502, so every bot fails its claim.                  
Revived a Vultr Postgres that was dead behind a Running status, then fixed a dead hover card and gave the phone a ship-up camera.                                                                                                                                                              
A run of mobile control work: the ship-up camera came and went, its d-pad stayed as a setting, and reverse became a fight-read on the stick.                                                                                                                                                   
The bot skill dial was three traits wired to nothing (reserve, greening, retreat) plus a bomb permission line at 0.35 carrying all of it. Wiring them took the bare-field ladder from -189 inverted to +141 monotone. Measure with the ablation's null; guesses were wrong three times.        
Where the bot skill work stands: the shipped ladder is still inverted, only permission grading and persistent lead error survived, the Elo gap is too noisy to judge by, and the fix works on the Wedge but not the Apex.                                                                      
Which knobs carry the skill dial, what the bare economy can and cannot test, and the last cliff whose inverted sign was holding the Apex back.                                                                                                                                                 
The dial's floor work: the pit could not measure aim so the ladder moved to alpha, matched kits made every hull separate, reaction-as-output-delay failed, and an aim floor below skill 0.30 makes genuinely bad pilots possible.                                                              
The graphics push: trails, wall light and nebulae landed clean, and bloom shipped broken on a viewport-ordering contract, got fixed, and waits opt-in for a screenshot.                                                                                                                        
Post-process bloom removed whole after it flickered in motion on the phone; geometry effects stayed. Trails re-cut to a 50ms clock and twelve samples, leaving from each hull's own jets. Explosions bend the starfield in a travelling band, never projectiles.                               
Hit and impact effects shipped, and the bomb work found that the drill was measuring the wrong economy while every bomb gate is wired to the skill dial.                                                                                                                                       
Game rethink (Splatoon-shaped: 3-min 4v4, modes, rivets): docs/design/progression.md proposes it, .design/rethink mocks six screens. A kit is 30 chosen greens replacing the random spawn deal, but must not count toward bounty or camping pays.                                              
Match game: docs/design/match-game.md settled it, sim core has kits (a vector over the old prize space) and bounty = base + run, modes::Melee owns the 3-min clock and 25s podium, rooms rotate maps at each whistle, S2C_MATCH=14. Client builds here with openjdk-25 + bob 1.13.             
Finished the hangar and the shop, and stood a local fleet with its own Postgres up to prove the rivet path from a kill row to a bought entitlement.                                                                                                                                            
Finished the match game's surfaces: the podium, the pockets, and a menu that looks like the design rather than like the old one.                                                                                                                                                               
The menu's pages match their mocks; the roster stopped being a tech tree and every trait is on the shelf; and friends shipped on the meta layer with presence read off the rated seat.                                                                                                         
Lessons from finishing the hull work: run luacheck before pushing, a hull change keeps its kit, and driving the built client locally needs a local directory in game.project and held mouse presses.                                                                                           
The ship page is one node with a hull carousel at its head; bots lost their seat at every map change; the rivets mark is a fastener seen from the side.                                                                                                                                        
The kit economy settled on its own Upgrades tab, and a playtest of it found charges firing every tick and a diagnostic endpoint refusing most reports.                                                                                                                                         
Friends presence is per room, the ship page became a header band over two columns, and a kit that outgrew its account is trimmed rather than refused.                                                                                                                                          
A long menu pass: the shelf moved onto the ship rows, bots started buying their own, and the standings became a real table.                                                                                                                                                                    
The front end this week: ladder prices on the ship page, a standings table with its own columns and filter, a decision to keep the menu vector rather than HTML, and a full mobile pass over both orientations.                                                                                
Admin edits wallets and upgrades; the friends page was mocked and then built: an add field, accept and ignore, and a sticky ignore in its own table.                                                                                                                                           
Menu and hangar cleanup: page scroll clamps to its own box, upgrades is a tab again, a joining pilot's first kit is dealt at once, and the ship page shows only what the account owns.                                                                                                         
Intermission rebuilt (whistle benches everybody at energy 0, map cycles at close_match, 15s, six fixed podium phrases by index). Map format v2: per-map w/h and 45-degree slopes; the admin editor publishes catalog.version+serial, and sim_map_check is the one verdict on flyability.       
The admin map editor grew from a wall-and-slope palette into a real editor: every tile class, stamped objects, previews, shift locks, pan, marquee clipboard and a hover readout, with a browser drive catching what unit tests structurally cannot.                                           
Spray and double barrel are one ladder (SIM_MOD_MULTI 0..5, slot space 23, protocol 17, decision 54); dropping an add-on also means trimming marks.lua's mod tables. The map check now measures connectivity with doors open. Friends page nav dies early when it has no rows.                 
One button outline (key_box); text_w takes the face, the menu's measured from menu_face.lua. A slot-shifting migration hit the PK per row and re-ran every restart. CI fails on any clippy warning now, beside fmt; wrong lints get an allow with a reason at the site.                        
Diagonal walls need a solid spine because one run of slopes is a one-way barrier; the menu ground became the arena starfield, and CI's newer rust caught the first clippy failure.                                                                                                             
The glyph pool was clamping the standings table silently, and the menu settled its hover rule, its entry guard, the week arrows and a Discord page.                                                                                                                                            
A run of bugs in map generation and round drawing, all found by pushing: compiler-dependent mapgen output, an 11 MB test stack frame under clang, and rounds identified by array slot.                                                                                                         
A day of client faults found by looking at what the screen actually did: prediction scattering pairs and arming fuses on guesses, and an upgrades page measured against two different right edges.                                                                                             
2026-08-23 client pass: Discord redrawn as a page, both match clocks ticking their last five with the bounty gone from the corner, map names on the wire and previewed through the menu, and ship deaths that expand instead of closing.                                                       
UI: bounty uses priced rivets; ripple removed as wreck-implosion source; landing is rail-flush, trims captions, labels sides, marks AI, puts hull in DEPLOY, bounds map. Native WS starts after publish, handles partial/backpressure, caps backlog at 4 MiB. Checks pass.                     
Bot fleet hardening: async single-flight/weak map builds, validated wire state, snapshot-stall/retry tracking, flight/hull shop gates, and private rotations. Walls: sparse station-industrial 45/90-degree modules, varied panels/warnings/pipes, stable while camera window moves.           
Wall direction is now plain: keep mass and readable edges, remove all interior decoration, preserve non-wall art. The 2026-08-23 churn audit removed dead paths, split safe modules, and kept network bounds/golden. Deployment tests must follow sourced fleet modules after refactors.       
