vectorwake (criccomini): Subspace Continuum inspired top-down space MMO, started 2026-07-31 in research phase; work is committed and pushed straight to main.                                                                                                                                  
Subspace research written to docs/research/ from the ASSS 1.6.1 guide, asss source, and nullspace; pushing to criccomini/vectorwake is blocked (403 on receive-pack, App API not accessible), reads work.                                                                                      
vectorwake architecture (docs/architecture/): Defold client for presentation only, shared deterministic fixed-point C99 sim core (native ext + WASM + server lib), Rust zone server authoritative over damage, WASM-sandboxed zone modules; push to main works.                                
vectorwake design: inspired-by not clone, clean vector art, 8 ships (Apex..Lattice), browser>Steam>mobile>consoles, Nakama meta-layer only; AI bots emit inputs only and yield to humans; rating is damage-weighted pairwise Elo anchored by rated bots.                                       
vectorwake: duel mode designed (1v1 first-to-5, ephemeral arena + module, per-match Elo, input-log replays); closed decisions: 100 Hz tick, nose aim only (buttons-only input), own protocol; license direction PolyForm NC or BUSL with CLA, counsel before public.                           
vectorwake bots are long-lived roster individuals (own ratings from archetype priors, schedules, slow careers, one-place-at-once); criccomini flies macOS + web, feel target Standard VIE, cadence autonomous with push to main.                                                               
vectorwake sim core (sim/): C99 fixed-point 100Hz with flight, energy, bullets/bombs, damage/death/respawn, events, golden-trace determinism harness on x86-64/arm64/wasm; code uses generic terms (baseline/units), no inherited jargon.                                                      
vectorwake playable web client (bare-clang wasm, one 43KB HTML, AI pilots through the input path); make lesson: command-line CFLAGS replaces makefile CFLAGS += entirely, so required flags live in a separate BASE var.                                                                       
M0 gate passed 2026-07-31 (determinism green on x86-64, arm64, wasm); controls are Ctrl guns + Tab bomb with WASD flight, fullscreen Keyboard Lock routing Ctrl+Tab to the game.                                                                                                               
vectorwake controls: arrows steer with the right hand, Space guns and Shift bombs with the left, Z/X and mouse as alternates; Ctrl and Tab were abandoned because macOS and Chrome own them.                                                                                                   
M2 gate met: Rust zone server authoritative over the C core via WebSocket, client predicts own ship, peak error 0.9px at 150ms RTT and 11KB/s per client; prizes and inelastic walls landed earlier.                                                                                           
Rating layer and duel mode both live on the server: identity-keyed damage-weighted Elo with verified multi-attacker splits, plus multi-arena zone with a Mode trait and first-to-5 duels against rating-matched bots.                                                                          
Offline duel and warzone flag game both live in the client and server; artifact must never depend on a server it cannot reach.                                                                                                                                                                 
M5 zone operator surface and the Defold client both done; Defold notes: bob 1.13 needs JDK25, .c compiled as C++ so headers need extern C guards, graphics module owns render constants.                                                                                                       
Defold client networking: sim.init must precede connect (snapshot carries state, not rules); websocket vendored at client/websocket due to hardcoded manifest path and blocked archive URLs.                                                                                                   
Defold: release variant strips draw_debug3d (line art needs a real mesh+material); and readPixels can't verify WebGL output from Playwright -- screenshot instead.                                                                                                                             
Bot skill was inverted by react gating the trigger; and Defold on_input action_id is a hash -- comparing it to a string swallowed every key. Verify input by asserting coordinates move.                                                                                                       
Client rendered the world mirrored (hull rotation reflected); and firing costs were priced off damage instead of the original's fractions of max energy, gating guns to 0.6/s against a 4/s delay.                                                                                             
vectorwake client rewritten to mesh-layer vector rendering (release now ships) and given synthesised sound; Defold shader cache is per-platform.                                                                                                                                               
vectorwake ships from artifact 8a789853 (Defold release web build); 459e61f2 is the older hand-written prototype and must not be overwritten.                                                                                                                                                  
Client joins zones from the start screen (NAME/ZONE fields, JOIN, Tab focus); two browser clients verified seeing each other. Background tabs throttle rAF, so test pages need bringToFront.                                                                                                   
Zone serves wss (tls_cert/tls_key); head-of-line cost measured: harmless at 40ms RTT, ~1 hitch/1.4s at 150ms+3% loss, bridged by coasting through the shared sim.                                                                                                                              
Clicks were never broken (sub-frame presses are dropped; hold ~90ms in tests), and names are now generated call signs rather than typed.                                                                                                                                                       
Map tiles are typed classes with a variant nibble, and maps load from RLE files the server sends to clients.                                                                                                                                                                                   
Two web-client traps: a stray 'nd' broke every build while 'build.sh | tail -1' hid the exit code, shipping 3 stale bundles (start screen now prints the commit); and Defold resumes audio only from a canvas pointer, so single_file.py unlocks on any gesture.                               
Bombs now splash on wall and direct hits alike (not timeouts, criccomini's call), golden trace regenerated; feed became a notification feed. Client CPU 40%->12% of a core: a Lua buffer-stream write is one C call per float, so vwbuf writes a whole shape per call.                         
No start screen now: land flying, escape opens a menu tree over the live arena, hull change is sim_set_ship_class (a respawn), page draws its own starfield while wasm compiles. A --fragment page needs its own viewport meta or a standalone phone lays out at 980px.                        
Zone hull changes work (C2S_SHIP, core-gated on alive+full bar, upgrades lost); the address field is gone, ZONES only. Weapons are one table model: bounce, proximity, splinter, push and stall all ship as rows, unused by the baseline zone.                                                 
Zones own their rules: settings (with the weapon tables) are packed by the core and sent after the map at join and on reload. Zone files write weapons by name; apply_config rebuilds from the baseline first, so a reload is idempotent and a deleted line reverts.                           
Tech tree: a level is a rung on a per-hull ladder of patterns; an add-on is a transform applied at fire time, per trigger, carried by the shot. Greens carry no type -- the roll happens at pickup over what the hull could ever hold, so every green is takeable.                             
Prize weights are relative and read against the hull's pool; rust corrodes only what a pilot holds, so it never punishes a fresh spawn. Charges are a pattern plus an inventory, with the ready slot in two button bits rather than in sim state.                                              
criccomini's Kinesis Freestyle2 ghosts arrows+space+turn; A/D works, and it is hardware not software. Bounty is derived from what a pilot holds, points are the victim's bounty paid to the finisher, so a fresh spawn is worth nothing.                                                       
Bots hunt by bounty/distance and chase greens when nobody is near, so practice arenas grow real bounties; projectiles retuned faster than any ship (bolt 800 px/s, shell 500, frag 400, burst 600) with lives cut to hold range.                                                               
Tuning comes from ASSS dist/conf/svs, not invention: rounds inherit ship velocity so 2000 vs MaximumSpeed 3250 is deliberate; multifire +50% energy +100% cooldown per rung; prize weights 40 stat / 25 level / 30 multi-shrapnel / 70 charge.                                                 
The original gates almost nothing per ship: charges (3 each), multifire, bouncing bullets, proximity and shrapnel are universal; its whole per-ship diff is nine settings (MaxBombs, ShrapnelMax, BombBounceCount, DoubleBarrel, EmpBomb, CloakStatus, SeeMines, BrickMax, Gravity).           
spawn_prizes (default 30) loads every spawn and flattens the bot skill gap, so calibration pins it to 0. The arena is the full 1024-tile map: a hashed 64-tile cell lattice round the old centre room, meshed by the client in a window round the camera.                                      
Two silent failures: a Rust mirror one size behind the C core is heap corruption (sim_sizeof_* guards it now), and an overflowing vec layer drops primitives, looking like flickering hulls not a slow frame (draws culled to camera; watch the heartbeat lost count).                         
Perf: snapshots halved (prize tile coords + 256-tile interest radius); sim_step is superlinear in weapons x ships and lost a third to spec caching and a compact hull array. Load time is already handled by the host's brotli.                                                                
HUD reworked: energy and bounty ride on your own hull, scoreboard and loadout behind one default-off 'info' toggle, hints in a menu help page, flags as pennants, and touch controls one bottom row -- guns, bombs, then a tappable pad per charge.                                            
Touch flight is a drawn d-pad, angle-sectored so the wedge between two keys holds both. The pointing stick could not express reverse (opening range with guns still on someone); a relative stick reading the same axes felt worse -- nothing to aim at.                                       
The touch stick asks for a course, not a facing: point behind you and the ship reverses, aim held. Forward/back re-arms when the thumb crosses the origin -- hysteresis was unreachable, since the hull chases the thumb and collapses the angle it reads.                                     
A thumb arcs rather than crossing the origin, so sweeps hold the rudder until it lands, timed in seconds not frames. Zone model redesigned (decisions 23-26): one arena per process, directories relay only own observations, arenas union views and pick a type when empty.                   
criccomini wants prose deslopped and concise, docs included: no em dashes, no punchy fragments, no bold-lead bullets (skill in .claude/skills/deslop). The touch stick reads a swipe against the nearest foe, who wears a reticle while pointed at.                                            
Mobile has no reverse, settled: five schemes all felt worse than the plain pointing stick, do not retry without a pad of its own. Duels removed for now: C2S_DUEL, the Duel mode, its Lua copy and Zone's arena map out, sim_map_duel is sim_map_pit. Decision 16 deferred, design kept.       
Web build in a fresh container: bob.jar only from GitHub releases (d.defold.com 404s), and bob 1.13 needs JDK 25 (class file 69) from api.adoptium.net. Debug server builds never started a zone: (i as u16)*8192 overflows at the 9th bot; release wrapped it, so nobody saw it.              
ASSS ships no per-ship balance: of 84 settings per ship only 10 differ, all capability flags. And a room is 79 KB (sim_map 1 MB is shared by pointer) at 1.6-16 us/tick, so 615-6400 rooms per core -- hosting cost is egress not compute, ~$1.56/mo per player on Fly, ~0 on Hetzner.         
Hosting is Vultr for everything (33 regions, managed Postgres, own IPs so Docker host-networking and UDP work); Fly rejected, browsers cannot header-route. ASSS had no per-arena cap: DesiredPlaying defaults to 15 and only opens another arena, so SIM_MAX_SHIPS=64 is stricter.            
SIM_MAX_SHIPS=64 is cheap to raise (255 costs 50 us/tick, 107 KB a room; move the header and MAX_SHIPS in server/src/sim.rs together). Repel is hostile only now: both push loops test team, which covers the firer, who used to be thrown at 484 px/s.                                        
Fleet design complete in docs: catalog.md (schema + validation), discovery.md (0x40 tags, JSON bodies, INTENT carries its own TTL), zones-and-arenas.md (selection algorithm, state machine, join path with teams decided by the mode), M7 build order. Room size is per-zone.                 
Fleet questions closed: no chat, DNS directories, dynamic rooms under max_rooms. Bots never fired a bomb (both or-ed BTN_BOMB onto a held BTN_FIRE, and a held gun wins); they now release the gun and only when the bomb out-rates it.                                                        
All eight hulls carry the original's identical flight and weapon numbers, so the roster is mechanically flat (K/D 1.15-0.89, was 2.86-0.27). A bomb level buys blast not damage; proximity is binary at 3 tiles. Four unmatched rules are listed in commit 47db494.                            
Bomb rules match the original (prox +16px a level, scattered shrapnel 2/4/8, 3 energy for a fragment's first 25 ticks) and every sim setting is now a zone.toml key, charges and shrapnel rungs included.                                                                                      
Hull ladder: with no greens all eight grade 0.95-1.03, so every difference is the tech tree -- freeze +0.15 K/D, multifire exactly neutral, Lattice trails on pool dilution. And bots never bomb: can_bomb compares damage per tick and the gun wins 8.00 to 5.00 everywhere.                  
Settings audit: flight, weapons and prize weights match ASSS exactly; MultiFireAngle (4.5deg vs our 15), BounceFactor and the prize economy do not. And the client now has a generated 8-bar synthwave loop -- Defold plays 22050/44100 Hz only.                                               
Deploying blind taught two things: ufw ships active on Vultr and drops rather than refuses, and a host's diagnostic channel must not sit behind the thing being diagnosed.                                                                                                                     
vectorwake went live on one Vultr box behind Caddy, then got a security pass: input bounds, dropped capabilities, scrubbed metadata, mail-spoofing DNS records, rotated tokens.                                                                                                                
tools/pilot flies real clients that decode snapshots through the sim core; it found the recharge overflow and, indirectly, a silent refusal loop that hammered the directory once a second.                                                                                                    
Bots now perceive like players (960px sight, 10-20Hz), and the deploy path was rebuilt after reinstalls burned Let's Encrypt's rate limit: one hostname with paths, updated by git pull.                                                                                                       
Playing Chaos found three stacked defects: teams=1 disabled all combat, a bot with nothing in sight froze, and greens were unfindable on a 1024-tile map.                                                                                                                                      
Greens were unfindable: uniform over a 1024-tile map at prize_max 20 is one per 50k tiles against 60 tiles of sight, so the tech tree was unreachable. Now a 6-28 tile ring around a live pilot; baseline 24 at 1/s or it carpets the ground.                                                  
Two more found by playing rather than checking: War's flags sat past everything anybody could see, and the server never answered a WebSocket ping.                                                                                                                                             
Two screens that looked fine and were not: the client had no S2C_KILL branch so ratings never updated, and the zone browser answered to keys only, so a phone could see chaos and war and tap neither. Rows publish hit boxes now.                                                             
Repel now matches nullspace's SimulateRepel exactly, and CI builds and publishes the server image to GHCR on every push to main.                                                                                                                                                               
The repo vendored the humanizer skill for all writing, and a stale client bundle taught the core to refuse any message it reads short of.                                                                                                                                                      
A second zone join left the first socket writing into the same sim, and Caddy's certificates moved onto a block volume a reinstall cannot take.                                                                                                                                                
2026-08-03 The flag tally left the wire, the banner being for sentences. And a sweep of dead weight from main: client-web (never compiled here), zone/directory.toml, zone/maps/arena.vwmap, zone.toml [[bots]] and [[staff]], and the Caddy DNS image built for a wildcard we dropped.        
Two things the fleet was doing silently wrong: the calibrated bot ladder never loaded (a path the image never filled), so it is include_str! now; and the roster was sent once on join over a try_send that drops, so a scoreboard read 'ship 5' all session. It repeats every 2s.             
Alpha reached the live fleet on its own arena, and getting it there uncovered an image build that had failed for four pushes because a compiled-in file sat outside the Docker context.                                                                                                        
Two things nobody could reach: a joining pilot got no spawn kit because join clears the seat and never re-outfits, and bots pinned on walls chasing greens because nothing tested reachability or noticed an approach that stopped closing.                                                    
2026-08-03 The ladder's 279-point spread was staleness, not skill: regenerated it gives 65 over six rounds. And the input timing fix shipped: server queues by tick, client leads on the acked tick, -2.7 to +3.7 ticks measured.                                                              
Offline mode removed: no local bots or browser screen, the menu is both home and pause screen (menu.home says if a game is behind it), games list polls every 3s. Its hull blurbs are now false: all eight fly identically, real diffs are add-on ceilings and Anvil MaxBombs 3.               
2026-08-03 To prove prod serves a commit: decode the page's embedded assets and compare game0.projectc, the only byte-reproducible one. And decision 29 designed: bots leave the arena for a bot server of declared clients, bot_fill 0.8 of max_ships.                                        
2026-08-03 Bot server built and merged: 'vectorwake-server bots' is a subcommand like 'directory', so calibrate and live bots cannot drift. Arena evicts the newest bot to seat a human; roster generated past nine. Costs 3% of the tick, 14% of a core at 51 bots.                           
2026-08-03 Detailed hulls shipped (plates, panel lines, canopies, nose-lit silhouette; vec.lua gained seg_glow and glow_band). Bots shipped live too: chaos and war hold 51, alpha 19, after fixing sequential status dials and a missed reply reading as 'wants zero'.                        
sim/tools/lvl2vw converts a .lvl to a .vwmap (format in docs/research/lvl-format.md): types to classes, starts derived from the largest open region. Alpha, Chaos and War run on criccomini's converted maps at catalog v4. tools/pilot's join packet was stale.                               
Alpha, Chaos and War run on maps converted from .lvl files at catalog v4. Ship bounce and weapon ricochet shared one event with different payloads, so bouncing bullets thumped on the shooter's hull; ricochet is its own event now, and silent by choice.                                    
Interface redrawn in the ships' language: chamfered brackets, corner stack, radar with range rings. Every map is closed by sim_map_index with a four-tile boundary, not by its file, since out-of-bounds solid cannot stop a hull that crosses a thin wall in one tick.                        
buffer.create mallocs without zeroing, and the renderer draws each layer's whole buffer every frame while erasing only back to the busiest, so vwbuf Attach must memset. Client and server images publish separately, the Rust one slower, so a protocol bump refuses joins meanwhile.         
Accounts live on the fleet: meta-layer on Vultr managed Postgres, rustls plus their CA in deploy/db-ca.pem; restart the bot server after it lands. Wall flicker was thin hard-edged strokes, fixed by a pixel of falloff in vec seg, which beat 16x multisampling.                             
Web audio lag was Defold's HTML5 device queueing 4 mixed chunks ahead; one-shots now go straight to Web Audio via html5.run at 2.7ms, loops stay on the mixer. Ships bank on turns: cos(roll) on local x in world.ship from smoothed heading rate, visual only.                                
Asteroids turn per frame (shape baked per seed, crater light in world space, 17 rocks = 5664 glow verts) and the corner stack grew a tile readout. Alpha now holds Chaos's room at sixteen a side, Chaos took the uploaded SERVER.CFG, and all three zones carry eight ship blocks.            
Map view live on main with catalog v7: M or a click on the radar swaps in the whole 1024 tiles, terrain only, opaque panel (a 0.55 wash leaks greens). sim.map_coarse in C, greedy rects in world.lua, ui layer 24576. Merge to main by pushing branch:main.                                   
CI luacheck lints the client Lua, catching a local used above its declaration, which is a nil global read. Radar blips blinked because a 2.8px hard square covers 2 or 3 pixel centres; fixed by whole-pixel blips and snapping the dial camera so the pattern slides rigidly.                 
Two silent-failure lessons: a Lua error inside a draw freezes the last frame rather than showing anything, and sim_unpack's memset means any edge-detected input has to ride the snapshot or it fires again on every one.                                                                      
Ten items shipped: about page, sortable scrolling scoreboard, Q toggles multifire, green doors, animated wormholes, own arrow on the map. VW_REPORT=0 stops an arena filing rated events with the meta-layer and nothing else; set on all three while the fleet is under test.                 
Home screen redesigned as 'wake': the name set large with a swelling stroke under it, and two hulls crossing on closed paths trailing wakes sampled backwards from time rather than a history buffer. Paths sized against the view so they clear the menu at any width.                        
The radar dial re-rolled every terrain rebuild because the sample stride took its phase from the window corner; anchored to the map's even grid now. And docs/design/audio.md records the kit, weapon rungs, deliberate silences, and which sounds are worth latency work.                     
Bots stopped grinding walls (56.2% of their lives to 0%) with capsule whiskers, bend-to-open steering and an unstick reflex. And client-defold.md was a pre-build plan gone stale, rewritten to the client that exists; decision 9 superseded, no tilemap was ever built.                      
Two parallel bot brains merged (router plus whiskers: Chaos 0.76 kills/bot-min from 0.08, grind under 2%), and the kill feed went authoritative off S2C_KILL with the payout riding it.                                                                                                        
Landscape phones fixed in two steps: scale_mode stretch hands the game the window, then a vendored engine template pins the canvas to a px-sized container (stylesheet !important, visualViewport) because iOS Safari keeps a stale intrinsic ratio and the engine strips inline style.        
Safari twice: the landscape stretch was disagreeing sizing passes, now a 4Hz viewport reconciler (canvas.width writes clear the frame); and with no Cache-Control phones cached stale builds, so the page sends no-cache and Caddyfile changes bump their compose label.                       
Safari stretched the canvas despite correct styles, so each tick verifies getBoundingClientRect and pins px when the box disagrees. And the pegged host was 153 bot pilots stepping private room copies; one shared Rig per arena with a pen holder cut 1.4 cores to 0.4.                      
pgrep -f matches the shell holding the pattern, so wait-loops deadlock on themselves (test liveness by port or pgrep -x); and the iPhone confirmed the rect-verify inline pin fixed Safari landscape, all three template layers staying.                                                       
Client batch shipped (explosions, rung colors, force-field doors, tumbling rocks, favicon, feed off touch, louder thrust, alpha 5 teams, wormholes 200/320); prod turned out to serve the --fragment build, so client.yml now folds the full document. --fragment is only for artifacts.       
Full view is a punted account+zone capability shipping with the visibility filter (house fleet and staff hold it); a filter buys economics, not secrecy, since sight-pooling is a scout team. And pgrep -f wait-loops deadlock on their own cmdline; test liveness by port or pgrep -x.        
Teams designed (decision 32: named, invite-only private, three caps) while alpha's numeric teams was resized live: 5 over the room's real 64 seats gave sides of ten, so teams=10 now caps a side at 7 at any fill. Divide the seats, not the player cap.                                      
Client hit boxes test in publication order (ship boxes deferred; names clickable, never hulls: left click is the gun); Safari bars tint from theme-color or sampled background, and the dark override sits below DEFOLD_CUSTOM_CSS_INLINE, which paints body white after the template.         
The field of play holds no hit boxes (left click is the gun), so asking who a pilot is belongs to the scoreboard row. And the games list redials its directory on a 2-to-15s backoff with a generation guard, having been a dead end that only a reload escaped.                               
Team names dodge the pilot and bot rosters so a side never reads as a person, and the generator rotates through its words rather than restarting, so founding after leaving gives a visibly different team.                                                                                    
The info box is where a pilot is described and where they are invited: it names their side only when the zone marked it public or it is your own, and carries INVITE when your own side is private.                                                                                            
A repel lives one tick, so a watcher is sent it only when the fire tick is a snapshot tick. And the arenas are cheap: 263/150/289 us a tick median across war/chaos/alpha, 7% of one core, with no humans playing at all.                                                                      
Debug readout wraps into columns at body size; bot marks align via text_w (DejaVu Mono 1233/2048). Controls audit: the deployed engine preventDefaults Tab (canvas keeps focus through held Shift+Tab), keyboard lock needs JS fullscreen not F11, Continuum's Shift is afterburner.           
The web client draws its own cursor, a chamfered arrow that fades after 2s of stillness, with the OS one hidden over #canvas. Metrics are published at /metrics/<service> behind Caddy basic_auth, the hash defaulted twice because a blank one won't parse and downs the site.                
The drawn cursor hides when the mouse leaves #canvas, polled through html5.run only while visible. Strangers' guns were silent because fire events exist only for the ship you predict; world.shots reads their cooldown rising with their round count, and splinters no longer fire.          
Decision 33 controls: arrows fly, Tab bombs, Shift guns; authentic Ctrl only under fullscreen keyboard lock (vwLocked). Digits 1-4 spend charges by corner-stack row, ready/cycle deleted, C V unbound, WASD removed, touch pads unchanged.                                                    
The help page draws the keyboard itself, rows in key units with bound keys lit by function, measured blind by board_test. Baseline shrapnel bounces now, 255 bounces as a mode: it ended on walls before, so wallward fragments died at birth and a rung read as 1 or 0.                       
The help board grew to 780 points with everything sized off key height, then the whole menu was redesigned: a rail of drawn marks beside a stage previewing what it points at, Chakra Petch as a second gui font, stepped settings, hud dimmed under the menu, scrolling lists.                
Two menu fixes: a tip box under DESTROYED (arena/tips.lua, drawn by ui.wait with the tick rule as respawn drain), and mobile nav, where rail taps were routed as rows of the current page and home_root moved the whole block on descent.                                                      
Hold-H HUD help shipped: a line beside each instrument, no leaders, anchors filed as each element draws, wash under the HUD. Defold loses a sub-frame press on keys and mouse alike. In-game screenshots run locally: bob, arena plus bots, a --settings pin, playwright-core.                 
2026-08-05 The DESTROYED wait box draws a card (icons on the alpha ui layer, respawn 400 ticks). Menu fixes: hulls navigate as a grid, right having been enter, so looking at a ship flew it; hover moves the arrows' cursor; the cursor is a blue field; the help legend replaces prose       
Hover names one HUD instrument, filed beside the anchors and kept out of M.hits so pointing never eats a trigger. And the wait card is cleared on the dead-to-alive edge, not the alive state: S2C_KILL beats the killing snapshot, and a close shot never exists in prediction.               
The interface shed its words: the menu got a ringed world, a hanging rail and an x out, INFO became PLAYERS on P, and the corner stack wears glyphs (miniatures for rows, what-it-does marks for add-ons, pips for depth). Hover and held H carry the words on request.                        
The favicon is the vee (client/web/icon.svg, base64 SVG + 64px PNG + 180px apple-touch); and 0699375 shipped game.project's directory as ws://127.0.0.1:9000 after a local test, killing the prod zone list. Shipped default: wss://play.vectorwake.net/dir; local runs use --config.          
Bots leave in phases and log off from a quiet corner. The sim sweeps weapon travel in 4 px samples so nothing tunnels a thin flank, remote hulls extrapolate on held movement buttons, and the render smoothing lies less and pins blasts to the hull they hit.                                
Enemy rounds bloom in over 120ms with a streak to their true origin (identity from owner, spec, birth tick; no wire change). And the unstick escalates: repeat pins relocate via nav.refuge, chained detours widen then yield. Bots froze up to 73s in travel; ~4s now.                        
The cursor-lag report closed as the rewrap-per-frame fix, and held-button extrapolation of remote ships was reverted the same day it shipped: bots steer bang-bang, coasting wins, smoothing back to 40 px and 80 ms.                                                                          
The logo lockup's mark got a visible stroke and a measuring test, and heading corrections now ease like position corrections always did, which turned remote rotation from 10-degree lumps into motion.                                                                                        
2026-08-05 Death cards are nine, adding shrapnel, safe zone and wormhole; walls never damage a hull so there is no wall card. The deck dealt 420 to 1620 of them: a 61-bit LCG product rounds in a double, so the low bits the picker read were rounding. Park-Miller 16807, top bits.         
Bots stop freezing when two share a spawn pixel, and the held H help overlay goes: hover names one instrument at a time now.                                                                                                                                                                   
Death cards name themselves over the body, words left and figure right. Corner stack shipped: 1.5x, strokes from the mark, rows aligned by ink centroid, arena level colours, gun is a line and a dot (three fanned, ring when it bounces).                                                    
A kill the local sim mistimed used to vanish silently, since unpack emits nothing and rollback swallows events; on_snapshot now diffs ships and rounds across the unpack and the arena draws what was missed. Zero silent deaths, from 15%.                                                    
Pilot and bot are one helmet: flat crown, chamfered jaw, person wears a brow and visor, machine two lamps and an antenna (unshareable, since three of four bot-mark uses are solo). Declined fan barrels are drawn dimmed but not counted as rounds, so one round means one ring.              
Controls cut to one key per job: guns Space (Ctrl under lock), bombs Tab, charges Q/W/A/S by stack row, multifire on backquote, Shift/Z/X unbound, menu key amber. Throwing that switch sounds, off a frame watching sim.ship_multi_off(me) rather than the key.                               
One helmet marks a pilot, with a brow and visor for people and lamps and an antenna for machines; the games list counts people with it. And the multifire switch is refused on a hull carrying no fan, so the key is silent there, while the flag survives losing the add-on.                  
2026-08-05 Two sessions merging .optmem at once left conflict markers in LOG.txt: records are fixed width, one per index, so repair is renumbering the tail and dropping the bad node. An empty games page draws range rings and a sweep, and the directory names what it is asking.           
Multifire clears when the last fan rung leaves (correcting #247), so the client wants a fan in hand as well as a change to click; and a zone with no arena wears the small sweep_dial where live rows show their population, no words.                                                         
Pilot and bot marks are one space helmet: a glass bowl on a collar, visor for people, lamps and antenna for machines, the visor bounded by the glass. Local main here is an unrelated history, so ship by merging origin/main into the branch and pushing HEAD:main.                           
Six ALT landmarks per map fixed bot routing: drill 5.3s to 0.9s a minute, zero failures. And the menu grew a generic confirm card: menu.confirm(head, keys), last answer safe, owns the keys; leaving a game is enter on the game you are in.                                                  
The router pass ended gameplay-neutral by A/B drills with departures now holding the door for teammates, and the bot server gained one clock per rig: a driver flies every seated brain while sockets keep the wire one client per bot, 15.2% of a core down to 9.7.                           
Pilot and bot marks split shells: a person is a round helmet whose visor hangs from a pivot above (below reads as a frown; the band is not convex, so quads), a machine a flat crown and straight sides. Collar, width and baseline pair them; capped strokes butt corners.                    
The visor swells from pivots either side of centre (bending both edges one way slides the band into a brow or frown, both rejected). And decision 13 amended: a short axis under 640 points backs the camera off to hold 640 world px, floored at 0.6, in client/render/zoom.lua.              
Metrics auth removed by request, /metrics/* open, the vw.metrics label firing a caddy recreate that severs live WebSockets. On touch the stack is one bounty row: weapon marks and ladders ride inside the trigger pads (pad_weapons, k=0.40r), pads_test measuring containment.               
The stick backs up: a committed pull within 0.61 rad of dead astern holds the nose and sends reverse, chevrons saying so; the cone tracks the heading so a turn never falls in. Touch screenshots need CDP dispatchTouchEvent held across frames; dmloader engine_arguments pin a serv         
Prod metrics are open and the empty fleet costs 0.83 of a core, so quote rooms rather than fleets. Case is decided in one funnel in ui.lua: the HUD shouts, the menu is sentence case, keys always shout, and names and codes are quoted raw.                                                  
2026-08-06 Mobile controls reworked (layout A): triggers wear corner-stack marks from arena/marks.lua, charges are square pip cells above them, stack drops weapon rows on touch. The rear-cone reverse was cut a day after shipping: glass has no reverse. touch_test.lua guards both.        
2026-08-07 A rung is now legible on sight and by ear: rounds coloured by pal.RUNG alone (team left round colour), and gun/bomb/blast each got four sounds climbing pitch, weight, drive and length. rung_test measures the colours, client/tools/sfxladder the sounds.                         
The pads and the corner draw one mark, marks.weapon, all six add-ons and no trail on an icon; pads had drawn 2 of 6, hiding shrapnel on a phone. Shrapnel ticks now count the zone's own fragments via sim.shrap_count instead of a ramp written into the drawing.                             
2026-08-08 Safe areas: #vw-safe probe wears env(safe-area-inset-*), window.vwInsets feeds ui.safe/touch.safe_l/r, bottom left uninset. And criccomini flew the kit: thrust 0.26 to 0.18, gun rungs widened to 7.6-8.4 by unpinning gun0's historical bytes and lengthening to 148 ms.          
