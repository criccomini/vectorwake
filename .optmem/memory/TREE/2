vectorwake (criccomini): Subspace Continuum inspired top-down space MMO, started 2026-07-31 in research phase; work is committed and pushed straight to main.                                                                                                                                  
Subspace research written to docs/research/ from the ASSS 1.6.1 guide, asss source, and nullspace; pushing to criccomini/vectorwake is blocked (403 on receive-pack, App API not accessible), reads work.                                                                                      
vectorwake architecture (docs/architecture/): Defold client for presentation only, shared deterministic fixed-point C99 sim core (native ext + WASM + server lib), Rust zone server authoritative over damage, WASM-sandboxed zone modules; push to main works.                                
vectorwake design: inspired-by not clone, clean vector art, 8 ships (Apex..Lattice), browser>Steam>mobile>consoles, Nakama meta-layer only; AI bots emit inputs only and yield to humans; rating is damage-weighted pairwise Elo anchored by rated bots.                                       
vectorwake: duel mode designed (1v1 first-to-5, ephemeral arena + module, per-match Elo, input-log replays); closed decisions: 100 Hz tick, nose aim only (buttons-only input), own protocol; license direction PolyForm NC or BUSL with CLA, counsel before public.                           
vectorwake bots are long-lived roster individuals (own ratings from archetype priors, schedules, slow careers, one-place-at-once); criccomini flies macOS + web, feel target Standard VIE, cadence autonomous with push to main.                                                               
vectorwake sim core (sim/): C99 fixed-point 100Hz with flight, energy, bullets/bombs, damage/death/respawn, events, golden-trace determinism harness on x86-64/arm64/wasm; code uses generic terms (baseline/units), no inherited jargon.                                                      
vectorwake playable web client (bare-clang wasm, one 43KB HTML, AI pilots through the input path); make lesson: command-line CFLAGS replaces makefile CFLAGS += entirely, so required flags live in a separate BASE var.                                                                       
M0 gate passed 2026-07-31 (determinism green on x86-64, arm64, wasm); controls are Ctrl guns + Tab bomb with WASD flight, fullscreen Keyboard Lock routing Ctrl+Tab to the game.                                                                                                               
vectorwake controls: arrows steer with the right hand, Space guns and Shift bombs with the left, Z/X and mouse as alternates; Ctrl and Tab were abandoned because macOS and Chrome own them.                                                                                                   
M2 gate met: Rust zone server authoritative over the C core via WebSocket, client predicts own ship, peak error 0.9px at 150ms RTT and 11KB/s per client; prizes and inelastic walls landed earlier.                                                                                           
Rating layer and duel mode both live on the server: identity-keyed damage-weighted Elo with verified multi-attacker splits, plus multi-arena zone with a Mode trait and first-to-5 duels against rating-matched bots.                                                                          
Offline duel and warzone flag game both live in the client and server; artifact must never depend on a server it cannot reach.                                                                                                                                                                 
M5 zone operator surface and the Defold client both done; Defold notes: bob 1.13 needs JDK25, .c compiled as C++ so headers need extern C guards, graphics module owns render constants.                                                                                                       
Defold client networking: sim.init must precede connect (snapshot carries state, not rules); websocket vendored at client/websocket due to hardcoded manifest path and blocked archive URLs.                                                                                                   
Defold: release variant strips draw_debug3d (line art needs a real mesh+material); and readPixels can't verify WebGL output from Playwright -- screenshot instead.                                                                                                                             
Bot skill was inverted by react gating the trigger; and Defold on_input action_id is a hash -- comparing it to a string swallowed every key. Verify input by asserting coordinates move.                                                                                                       
Client rendered the world mirrored (hull rotation reflected); and firing costs were priced off damage instead of the original's fractions of max energy, gating guns to 0.6/s against a 4/s delay.                                                                                             
vectorwake client rewritten to mesh-layer vector rendering (release now ships) and given synthesised sound; Defold shader cache is per-platform.                                                                                                                                               
vectorwake ships from artifact 8a789853 (Defold release web build); 459e61f2 is the older hand-written prototype and must not be overwritten.                                                                                                                                                  
Client joins zones from the start screen (NAME/ZONE fields, JOIN, Tab focus); two browser clients verified seeing each other. Background tabs throttle rAF, so test pages need bringToFront.                                                                                                   
Zone serves wss (tls_cert/tls_key); head-of-line cost measured: harmless at 40ms RTT, ~1 hitch/1.4s at 150ms+3% loss, bridged by coasting through the shared sim.                                                                                                                              
