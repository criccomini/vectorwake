Chris found predicted-death explosions jarring (server revives, hull pops back); decision 40: sim_settings deathless+mortal_ship, client clamps remote hulls at 1 energy, remote death fx wait for snapshot confirmation via snap_deaths. Server baseline zero, golden unchanged.              
Bot departure deadlock fixed: a parked leaver sat limp and got camped forever, so company now un-parks it; test loops 8 salts. Raw curl to api.github.com 403s here, so read CI via GitHub MCP tools, one workflow at a time with small per_page.                                              
Safari/iOS WebTransport has only datagrams.createWritable(); Chrome/Firefox only the deprecated .writable. Client now takes either. Vendored the Clanker Constitution as CONSTITUTION.md (v2026.08.10), referenced from AGENTS.md, which CLAUDE.md symlinks to.                                
Reroll stuck after 30 names/hour: /v1/rename throttles per IP and 429s. menu.M.reroll now takes the confirm card, shows "Rolling." in flight and puts the server reason on its head, like send_claim/send_login. 30/hour cap unchanged; menu_nav_test covers it.                               
Gunners shipped to main: carrier byte, sim_attach, C2S_ATTACH=10, drones on a per-hull ring. Subspace's Terrier was the only DoubleBarrel ship: 2 rounds, 4 with multi (it adds); Facet has it via class_row.gun_barrels, spacing nonzero or count>1 scatters as shrapnel.                     
Gunners: detach is the d key (net.attach(255)) gated on menu.typing(), your own drone wears your hull's halo, drones slightly smaller, drones_test.lua pins the ring geometry and the four-entry rider stride.                                                                                 
Local main here is a stale clone unrelated to origin/main; land via git push origin <branch>:main, and expect OptMem TREE conflicts from parallel sessions. On alpha the Facet's second barrel made multifire worthless (58.3% to 39.6%) and cuts time-to-kill 40%.                            
Mines shipped as charge slot 2: spec bytes 'still' and blast_up, and an enemy repel turns one into a bomb of its rung. pack.c hand-mirrors the spec struct, so a new field missing there reaches clients as zero; CFG_VERSION 11 and a memcmp guard now catch it.                              
Powered A/B (n=384/arm): Facet multifire 54.9% to 34.6% with the second barrel, z=5.78, shots/kill 8.32 to 5.46; no other row survives Bonferroni. Kill payouts now float: ui.payout draws +paid from S2C_KILL in pal.PRIZE, rising off the wreck.                                             
Mine life is 12000 ticks and ours to choose (VIE has no readable default), and an unknown weapon name in a zone file creates a weapon rather than warning. mapgen redrawn for a 3-tile hull with centred shapes; alpha ships seed 28, drill-picked.                                            
A new map needs a drill A/B (51 bots, 180s, 12 seeds), not a density check: bot stats swing 0.60-0.98 kills/bot-min map to map. VIE spawn default is a 37-tile square at centre (WarpRadiusLimit=20 clamps the formula); Alpha runs spawn_radius=18, 60 clears a camper's radar.               
Admin panel: accounts.admin flag, /v1/admin/* routes, deploy/admin/ page, Caddy {$VW_ADMIN_HOST:admin.localhost} guard; grant is curl-on-host, reply admin field is decoration re-checked per action. CCR env: VULTR_API_KEY, no vultr-cli; curl the API. play=155.138.225.232.                
Admin panel live and verified (meta stamps 3f67982; main pushes race the prod tag, read vw_build_info first). VW_ADMIN_TOKEN then retired: no route writes accounts.admin, operator SQL on central only, since neighbour containers can call loopback routes but not the DB.                   
Bare hull balance is hitbox area r=-0.91 (one shared flight struct; ships.md per-hull table unimplemented); Facet 2nd barrel +20.7 pts. Runbook SQL must be executed before shipping (grant returning bug); apt-get install postgresql works here for a throwaway cluster.                     
Death cards gone; arena/guide.lua teaches by trigger: asks name a control and retire on the doing, tells fire on sim events. CARDS gained key/pad. EV_WARP is two events (b==1 wormhole, b==0 door), now in Lua; EV_PRIZE b is the flat prize index and v is +1/-1 since greens rust.          
American spelling is house style per AGENTS.md; the repo is still mostly British and the sweep is its own change. origin/main gets force-pushed to unrelated lineages here (9a3ebab -> 83cd409, no merge base), so re-fetch and check merge-base before landing.                               
Multifire touch cell: touch.has_fan + fired_multi, spent as BTN_MULTI. Panel dashboard: /v1/admin/fleet relays the directory view over loopback (O2D_FLEET, refused when X-Forwarded-For present since Caddy proxies from 127.0.0.1), /v1/admin/bans, catalog version and key check.           
pawn radius is sized by seconds of bullet flight to the nearest enemy, not radar reach: Alpha needs 250  | Alpha hull matrix 0/30/60 greens: spread 19.2/10.9/10.9, hitbox r -0.87/-0.80/-0.53, flattening spent by 30. Wedge never improves, last throughout.                                 
Ranking landed: idempotent ingest (arena-minted unique event id), tiers Green/Wing/Lead/Ace/Legend drawn in the pilot panel, bot-only events swept after 3 weeks, AI gain capped 50/day above Ace (per room, friction not a lock), no carrier share by decision, VW_REPORT=0 removed.          
Operator verbs: /v1/admin/command relays O2D_COMMAND, actor from the DB, VERBS checked first; pin travels in Status. No automatic drain (DRAIN_GRACE never existed). Pilot edits: /v1/admin/rename (dealt only, bots refused) and /v1/admin/note; rating/password/kind stay fixed.             
Typed admin renames replace notes; uniqueness from the lower(call_sign) index. meta.rs serve() maps a fixed status list, so an unlisted code ships as 500. Fleet view: rooms whole, build on Register, sparklines. Three of the five server.md metrics were never measured; now via Rate       
Panel gained admin grants (claimed humans only, last admin protected), a live-filtering pilot table, and instance rows linking to the provider console via VW_HOST_ID from the metadata service. Deploy race: the page ships before the binary, so it must tolerate old field shapes.          
Guide port review caught a crash: guide_anchor wrapped e.text, nil for card-based ship-anchored tells. A zone's spawn_radius leaks into all three calibrate harnesses: Alpha's 250 on a 32-tile pit throws pilots out of the room and halves kills. Held at 0 now; it skewed the ladder.       
The frozen-HUD day had a second cause: a guide card line with no text crashed M.hud on a pilot first green. Moot now, since the trigger help system, hover label and death cards are all removed at Chris request. ui.row_at stays for stack_test, and the menu keyboard page stays.           
arena/controls.lua is the one list of controls (key, name, what, pad): ui.lua draws it as a table on H or ?, the menu help page builds its touch rows from pad. text_w counts bytes so the arrow glyphs need a codepoint count. Tests check it against game.input_binding.                     
Pilot event log (decision 42): per-connection session on Seat, generic Spool<T>/Spools, pilot.jsonl to /v1/pilot-events, no IP by design; panel reads it at /v1/admin/events under the pilot card. Rig: pg 5433, meta binary, python /v1 proxy, playwright on chromium-1194.                   
Rating tiers renamed: Newb/Wing/Lead/Ace/Legend (rating.rs TIERS, net.lua, rating.md). Pilot log gained died/kill rows (humans only, budget-exempt) and SIGTERM files leave why=restart per seat: converges had cut sessions to a bare join. Alpha is FFA, so no team events.                  
Pilot table shows rank/rating/tier from the most-played class per account. Cipher topped all six maps (mean 68.1) on geometry alone: 12px wide vs 20-34, no cloak in the core. Priced at 1300 in alpha, spread 23.6 to 8.8, still strongest with one counter; 1100 overshot to last.           
Two bugs kept arenas from filing anything: the spool was aimed on n%3000==0 so unarmed for a process's first 30s, and meta::call stripped only http:// with no TLS, so an off-host arena on https parsed a host of 'https:'. Both silent; the fleet looked healthy.                            
Admin: four hash-routed Fleet/Pilots/Activity/Access views, nav rail, stats, 25-row paging; totals use counts, events limit+1 at ~300k/day, sticky th fails in x-scrollers. Hero: three 4.3s battle windows in one 13.8s 960x540 WebM, 766 KB vs 1.2 MB.                                       
The homepage founder letter sits after the hero, Chris's wording intact. respawn_delay 400->1200 in 4v4 changed nothing: deaths fall uniformly, spread and Cipher flat, the deaths-help-you correlation was aggression confounding. Hull tuning ends: 48-53 across seven is done.              
Vectorwake site mark: aligned 3-stroke rows, top orange/dark gray/dark gray, bottom dark gray/cyan/cyan; gray #3f4b60, horizontal cuts. Keep compact beside wordmark at 18x22 px with a 9 px gap in header/footer.                                                                             
Vectorwake identity is shared by site, admin, play app, and game: aligned 2x3 wedges on #05070c. Use the heavy cut for favicons and ordinary cut for install/game icons; animate both in-game rows together on one clock.                                                                      
Two-row identity shipped and verified live: solid, flat-ended animated game wakes; public/admin header marks use a 0.6-unit outline; favicon/install assets keep approved cuts. Client/server-image CI passed and live assets match sources.                                                   
spawn_radius jitters around each side's spawn point, Alpha at 60. bot_token now calls /v1/session not /v1/login (login 403s a secret), so bots hold accounts in rooms, bot-involving rated events file, and durable bot careers move; proven on a local meta+postgres rig.                     
Never hand-merge .optmem/memory: Git can splice LOG.txt records and crash memo; take main's whole store and re-note through the tool. The founder letter CTA is 'Start playing now!' linked to play.vectorwake.net; Discord remains the feedback link.                                         
Ghost-room fix: net.lua quiet clock reports a dead server instead of redialling, and wt.rs closes its endpoint on SIGTERM. Discord is a rail stop opening the /discord redirect in a new tab; adding it exposed the rail overflowing a sideways phone at 7+ stops.                             
Discord brand rules forbid recoloring or reconfiguring the logo (Blurple/black/white only), so the rail mark is the official path traced, triangulated with eye holes and baked as verts+indices in ui.lua, drawn solid Blurple and never tinted by selection.                                 
Public site: hero says 'Massively multiplayer space combat'; deck says top-down multiplayer and free on desktop/mobile; GitHub/Discord open new tabs; /pilots uses stats-only APIs and persistent career totals for humans/bots; homepage adds a founder letter and Features.                  
The in-game rail Discord mark uses a traced path with faint fill and stop-color outline plus hole-aware triangulation; public-site Caddy route changes must bump compose's vw.site label so the updater recreates Caddy, since static files update live but routing loads at startup.          
Vectorwake uses PolyForm, so public copy says "free and source available", never "open source". Client links open on web through window.vwOpen in engine_template.html, a clicked hidden anchor; sys.open_url fails on mobile web and is native-only.                                          
Public site copy: "free and source available", pilot call-sign caps preserved, /pilots opens on search plus table. Opening a tab from Lua is blocked on every phone, so links are real DOM anchors the page lays over the canvas via window.vwLink fed by ui.link_dom.                         
On /pilots the search and table sit flush under the fixed header, whose border is the only top divider. The icon tile is a plain square: the two chamfers are gone from all four sources, PNGs regenerated with cairosvg, and logo_test pins the base64 embeds to the sources.                 
Mines became the bomb trigger's other posture rather than a charge: shift+tab or a touch cell, limited only by mine_max live mines per hull.                                                                                                                                                   
SVS mine settings: MaxMines=5 on all eight ships, MineAliveTime=12000, LandmineFireEnergy=270 +150/level, LandmineFireDelay=125, TeamMaxMines=12, SeeMines only on Weasel. The 20 in the schema is a ceiling, not a value.                                                                     
Identity rebinds by rejoin: net.joined+identity_moved+last_join drive enter_game to redial the same game on mid-game login/reroll/logout-guest; costs the ship, claim free, the reroll card warns mid-game. Noted twice across a store rebase.                                                 
Key rebinding shipped (binds.lua routes a key to a control's name, conflicts swap, esc fixed); star flicker was vertex truncation, so both world layers now size from hw/hh via world.world_budget and the debug readout shows fill and drops.                                                 
Launch plan advised: legal sign-off, capacity, Discord ping role first; then Subspace crowd, Show HN determinism writeup, flight nights. Homepage features are Chris's dictated copy, 8 cards; don't reword his marketing text.                                                                
Lua caps a function at 60 upvalues and nothing local counted them: arena.script update hit 62 and the bundle failed 4 commits while lint stayed green. Constants moved to a TUNE table, static_tx/ty onto static, and upvalues_test.lua now runs luac5.1 -p.                                   
Controls board publishes hit boxes so a click binds the hovered control; esc/caps/ctrl/enter/bksp unbindable. Site nav Discord button wears the official mark SVG tinted currentColor at 19x15, Chris's call, same as the rail.                                                                
Snapshots carry only what a client could lawfully see, culled to a declared interest radius (C2S_VIEW, 312 to 15 KB/s), which closed a maphack. Bindings are chords: keys held together, most-specific match on the press, Shift+Tab the mine and Tab alone the bomb.                          
The interest cull landed and both halves were closed out: its client-side fallout fixed (prize sowing, absent-seat colors) and its window proven live by pilot --view's reach column.                                                                                                          
Two measurement traps found while confirming the cull: a cancelled CI image build meant production ran older code, and pilot's predict_err had two bugs that cancelled out.                                                                                                                    
Two CI/wire traps: image.yml starves (3.2 min build, cancel-in-progress kills overtaken runs, no retry or prod-vs-main check), and sim_pack_around culled a pilot's own mines, so the client drew vanished rounds as blasts; a viewer's rounds now skip the radius.                            
Client review found 24 bugs, all fixed: watching crashed update() (ship_mod on me=255); sim_unpack memsets before validating, so ApplySnapshot now decodes into scratch and a refused snapshot calls lost(); pack.c refuses bad cls. Guards: save_guard_test, constant_drift_test.             
Logo unified site/admin/play/game; flat paths and widths pinned by logo_test. Client fixes: account races, touch focus/releases, audible SFX budgets, terrain overflow units, failed joins, snapshot ordering. Spectator guards already on main. 31 Lua tests.                                 
seg_flat was the one stroke with no antialiasing or minimum width, so the mark's diagonals rendered thin and stepped beside the name; it ramps like seg now, stretched horizontally to land a pixel square to the lean. stroke_test pins ink, ramp and floor.                                  
single_file.py boot loader is a fourth logo drawing; logo_test now pins it to the flat ui.lua constants and widths. Chris requested a hostile-system review of zone state/protocol/ratings, account/admin auth, bots, and deploy/recovery.                                                     
Review found auth, DoS, spool, bot, secret, deploy, and recovery flaws. Bombs stay predicted; remote deaths stay confirmed. DEATH? checks suppressed lethal calls against six snapshots as confirmed/settled plus pending, censoring hulls that leave sight; visuals unchanged.                
User approved a desktop-only, first-zone PRESS H FOR HELP prompt on main: bottom centered, slow gray-to-white pulse, menu-style close mark. H or the button opens help and saves dismissal; the close mark dismisses without opening.                                                          
AI review: bots lack recover/disengage; threat math ignores bot velocity; drill labels deaths as kills. Class 7 is stale from retired eighth ship Meridian, akin to SubSpace Shark, causing offline Apex fallback versus live Lattice clamp.                                                   
2026-08-13 Network lifecycle: first snapshot receipt fixed false lag; fly/watch protocol still lacks an epoch, so one-shot WELCOME, the connection-level snapshot gate, heuristic rewinds, and stale input/snapshot crossover can desync lifecycle state.                                      
Moved the fleet from Atlanta to Dallas, and found that fleet.sh could not see an apex DNS record because Vultr omits its name key.                                                                                                                                                             
Finished the Dallas move by tearing Atlanta down: atl1, its DNS and volume, the stale records, and finally the ATL database.                                                                                                                                                                   
Fresh joins seed 8 idle ticks and wait for current+2 future inputs before silently allowing flags. Input misses then use an exact 50-tick window with no path recovery delay, so a browser pause ages out within 0.5s. Debug labels this INPUT MISS.                                           
Fixed lag false positives by widening the server's input receipt window to 128 bits, and set the Discord icon to the canonical mark.                                                                                                                                                           
2026-08-13 Alpha favicons use the mark; Discord/install icon.svg stays opaque and its separator needs clipping. Lag ignores RTT/jitter/D/F/input-miss telemetry; silence releases weapons at 250ms, controls/objectives at 1s, spectators at 15s, and a valid packet recovers.                 
Filtered state is not authority: exclude sparse-heartbeat bots from human input-miss telemetry, and let Players/D send attach requests for roster-confirmed teammates outside the snapshot; the server alone gates target life, carrier state, capacity, and full energy.                      
Mobile keeps course steering unchanged: held reverse pad, upward gun pull for multifire, mine attached to bombs, and fixed charge positions. Capture it from /tmp with hasTouch/isMobile; orientation changes may require rejoining.                                                           
Greens can end up inside walls on alpha: sowing through an open door (solid() asks about a tick, ground() about the tile), plus an unfound path. update_prizes now sweeps any green off non-ground. Client draw code can be rendered offline via scratch wallshot.lua.                         
Two wire bugs: (v>>12)<<12 tile rounding is UB for negative v (tile_floor masks now; crashers pinned in sim/fuzz/crashes), and alpha snapshots are a median 1838 bytes so 62% ride uni streams, which UNI_INFLIGHT=2 rationed into the 'stream stalled' ejection. Now 16.                      
Client steps a filtered world, so shared-rng draws for unseen hulls desync it: respawn placement did (9416 px correction), now seeded per hull. Live vectorwake: 241 human accounts, 67% never scored, median 3 games, 2 past 100. Arrivals fine, first session is the wall.                   
Door crush warp asked what tile the ship's CENTER stood on, so a hull in a blank spot inside a door structure froze there; it asks the hull's box now, same point-vs-box class as the greens. Then HUD hover cards for the corner stack: name, greens on it, key that spends it.               
The flight HUD is DejaVuSansMono at 26 and glyph_w is built on its 1233/2048 advance, so a render in menu.ttf is the right box round the wrong letters. Prod alpha has no bots because the meta-layer is 502: every bot fails its account claim and never dials.                               
A Vultr managed Postgres can be dead behind a Running status: usage reads zero across disk, memory and cpu, and the endpoint resets. There is no restart in the API or vultr-cli; StartMaintenance migrates the service to a new host and is the only lever. It worked.                        
Found the hover card shipped dead because Lua tests cannot deliver pointer events, and added the phone's ship-up camera behind a setting.                                                                                                                                                      
Tried a ship-up camera on mobile and dropped it, keeping its d-pad as a steering setting beside the stick.                                                                                                                                                                                     
Rebuilt mobile reverse twice: anchored and ramped the d-pad, then replaced the reverse pad with a fight-read on the stick that tail-steers while an enemy is ahead or the trigger is held.                                                                                                     
Measured twice, the bot skill dial is not a range but a step at 0.35 whose sign the prize economy decides: greens off, 0.30 wins 87% on thrift; greens on, it loses 88-93% to everyone. From 0.45 to 0.90 every pair is a coin flip: aim_err, react, look and tol separate nothing.            
Fixed the bot skill dial by wiring three traits that were flat constants: energy reserve, greening, retreat threshold. Bare-economy ladder went -189 inverted to +141 monotone. Discipline then cost it, +141 to +88: retreating earlier never finishes anyone in a bare field.                
The shipped ladder still holds the pre-fix inversion and needs regenerating; of everything tried on the skill dial only permission grading and persistent lead error survived measurement.                                                                                                     
Two corrections to the bot skill work: the Elo gap carries about thirty points of noise so win rate is the instrument, and the dial fix was measured on the Wedge and largely fails on the Apex.                                                                                               
Ablated on both hulls: aim carries a bare field and energy discipline carries a built Apex, while a built Wedge has no knob at all.                                                                                                                                                            
What the bare economy actually gives a pilot, and the last mine cliff whose inverted sign was holding the Apex back.                                                                                                                                                                           
Skill separates on every hull once the kit is matched and the room is big enough to have a lead worth misreading; the pit was neither.                                                                                                                                                         
Two floor findings: output-delay reaction breaks the steering loop and was reverted, and the dial saturates below 0.30 until the aim floor (clean-look ramp plus held bearing error) makes a genuinely bad pilot possible.                                                                     
Shipped the graphics push (trails, wall light, nebulae, latched bloom) and learned this sandbox's proxy resets browser tunnels, so live screenshots are impossible here.                                                                                                                       
Bloom shipped broken because set_viewport must precede set_render_target; found via a forum thread with the identical scale symptom, fixed, and left opt-in pending a screenshot.                                                                                                              
Killed bloom for flickering, then made the ship ribbon visible and moved it to each hull's own jets.                                                                                                                                                                                           
Renderer effects landed together: ship trails now leave from each hull's own jets, and explosions bend the starfield in a band travelling with the ring, scaled by parallax depth. Projectiles never bend; differential displacement lies about a threat where camera shake does not.          
Two effects shipped correct and invisible, so every effect now gets measured the player's way: how many pixels lit, for how many frames, on a phone view. Under that rule shipped fx.flash at muzzles, wall impacts and struck hulls, plus rims grading toward HURT.                           
Bombs: the drill was measuring bare ships while alpha runs built ones, and any gate that raises bombing flattens the ladder because bomb frequency is wired to the skill dial.                                                                                                                 
Game rethink: docs/design/progression.md proposes rivets, standings, livery; Chris steered it Splatoon-shaped (3-min 4v4, several modes, rivets buy stats/add-ons/charge slots). Synthesis offered: a kit is chosen greens dealt at spawn, flat 30 budget, replacing the random deal.          
Rethink mockups in .design/rethink (build.py writes 6 artboards from palette.lua/ui.lua/ships.md/sim.h; seeded canvas gitignored). They found a hole: a kit counting toward bounty makes every spawn worth 30 and camping pay, so in a match zone bounty counts only kills.                    
Rethink settled in docs/design/match-game.md: only 3-min 4v4, no Alpha/practice/rotation, kits (a vector over the old prize space) replace greens, bounty = base + run paid to its ender as rivets, charges match-scoped, mines charge slot 2, turrets cut. Sim core done, CFG v16.            
Match mode server-side: modes::Melee owns a 3-min clock + 25s podium (controls held), the room opens each match with sim_restart (kit+ammo re-dealt), zones name maps=[..] and rotate at each whistle, S2C_MATCH=14 carries clock+score. Client builds here with openjdk-25 + bob 1.13.        
The hangar's kit page got its head and its bar, and the meta layer's rivet path was verified end to end against a local Postgres.                                                                                                                                                              
The local fleet runs out of the scratchpad run/ dir: up.sh brings up directory, meta, arena and bots, serve.py gives the page and the meta layer one origin, cat/ aims the catalog at localhost.                                                                                               
The intermission became a real screen, and the melee pockets sit where the camera and the flight-time test both hold.                                                                                                                                                                          
Redrew the menu in the mocks' language, and learned that a scripted keypress has to be held about 350ms or this client never sees it.                                                                                                                                                          
The menu's pages match their mocks, and the roster stopped being a tech tree: every trait is a kit slot the shop sells, and a hull is the shape it presents.                                                                                                                                   
Two silent gaps found by playing: a kit only reached the arena when a point was spent, and a purchase left the token stale. Friends then shipped on the meta layer, with presence read off the rated seat.                                                                                     
Two things the hull change and the linter each turned up: a hull change no longer wipes the kit, and luacheck (which CI fails on) found a nil-global read that had silently disabled a name size.                                                                                              
Driving the built client locally: point game.project's directory at ws://127.0.0.1:9000 before building, and press with a held mouse rather than a zero-length click.                                                                                                                          
The hangar and kit menu nodes are one node: NODES.hangar is a hull carousel as its first row with the kit ladders under it. No kit level, no roster grid, no click_hull.                                                                                                                       
Two from the second playtest: bots lost their seat at every map change, and the rivets mark had to be a fastener seen from the side to read at price sizes.                                                                                                                                    
Purchases stay on their own tab, renamed Upgrades, and nothing is granted: every slot the arena has is a slot the shelf sells.                                                                                                                                                                 
Two client-side findings from the burst playtest: charges now fire on the press, and /v1/client-debug rejects most reports with 400.                                                                                                                                                           
Friends presence is answered for the room you asked from, so the client forgets it on join and leave and says 'asking' until the new reply lands.                                                                                                                                              
The ship page became a header band over two columns of kit, and a build that outgrew its account is now trimmed rather than refused whole.                                                                                                                                                     
Two client fixes landed together: client-debug stopped refusing negative leads, and the upgrades tab folded into the ship page's rows.                                                                                                                                                         
Bots buy their own kit through the player's own endpoints, and the standings became a table you can sort, filter and page back through.                                                                                                                                                        
Two front-end passes: the ship page prices each ladder rung in place of upgrade buttons, and the standings table took the whole width with new columns and a real filter box.                                                                                                                  
The menu stays vector rather than HTML (Switch cannot composite a DOM over a live frame), and got a full mobile pass: height-aware layout, scrolling pages, a packed standings table, and corner buttons on every layout.                                                                      
Admin can now edit wallets and grant or revoke upgrades; the friends page redesign is mocked but not built.                                                                                                                                                                                    
Friends v2: an add field, accept/ignore, and a sticky ignore in its own table; the space bar doubles as the menu's go key so any submitting text line has to suppress it.                                                                                                                      
Two menu fixes: the page scroll now clamps against the page's own box rather than the stage, and upgrades is a tab beside Ship again with the ship page carrying no prices.                                                                                                                    
A joining pilot's first kit is now dealt at once rather than held to the whistle; the ship page shows only slots the account owns, chips take arrows as nav, and the add field completes call signs.                                                                                           
The intermission became fifteen seconds with something in them: an emptied arena on the next map, a scoreline card, a spoken countdown, and six fixed phrases anybody can press.                                                                                                               
Map format v2: per-map w/h plus 45-degree slopes deflecting by a velocity swap; melee maps are seeds 7 and 11. Admin map editor publishes catalog.version+serial to the directory, packing .vwmap in JS byte-identical to pack.c; sim_map_check is the one verdict on flyability.              
Map editor grew undo/redo, eraser, then the full palette and tools: every write goes through put(), which made history, symmetry, preview and clipboard all fall out of one choke point.                                                                                                       
Two panel bugs the browser drive caught and no unit test could: admin.js and maps.js share one global scope (a function vs a let is a file-killing SyntaxError), and a readout reserving less height than it fills shifts the canvas under the pointer.                                        
Spray and double barrel merged into one ladder: SIM_MOD_MULTI 0..5, a rung is a round, first rung keeps the pair's spacing. Slot space 23, protocol 17, spray_merged migration, decision 54. Dropping an add-on also means trimming marks.lua's MOD_DECOR, MOD_RADIAL, mk_bolt.radial.         
The map check measures connectivity with doors open now, reporting regions_shut as a note, so a door can be the only way in. The friends page gives up on the arrows when it has no rows, so keyboard stops there go above that check.                                                         
Buttons wear one outline: a stroked box (key_box in ui.lua); the chamfered bracket is for fields, cards and panels. text_w takes the face now: the menu's is proportional, measured from client/arena/menu_face.lua, and the mono advance runs a fifth long on it.                             
A slot-shifting meta migration hit the PK per row and its rollback took a run-once mark, so restarts re-ran it. CI fails on any clippy warning now (--all-targets -- -D warnings) beside fmt; wrong lints get an allow with a reason, sim.rs the only file-wide one.                           
Slopes as barriers: a single run is one-way (continuous face, discontinuous material), so a real diagonal wall needs a solid spine with a face either side.                                                                                                                                    
The menu ground is the arena starfield with charge-key boxes on the ship page, and the clippy gate first went red on a lint only CI's newer rust could see.                                                                                                                                    
A run of slopes needs a spine, and the glyph pool was clamping the standings table silently while the menu learned one hover rule.                                                                                                                                                             
Menu rules: rail hover only lights, down refuses a page with nothing to stand on, left/right on the week's table step the week, and Discord is a page whose first row carries the link.                                                                                                        
Two mapgen faults found by pushing the melee rework: mirroring and the density metric both ignored slopes, and the clang -O0 CI job died of an 11 MB stack frame in test_sim's main rather than any divergence.                                                                                
Two faults in code I had just touched: mapgen drew different arenas under gcc and clang from unspecified argument order, and the client drew double-barrel rounds at each other's positions because it named a round by its array slot.                                                        
Same day: fixed prediction clients arming prox fuses on coasted remote hulls (arming reaches 49-157px by rung, decision 40 covered deaths only), then landed the rethink on main at 7ed5ae0 merged with the melee-slope and menu work, all suites and sim check green.                         
Two upgrades-page faults and one wire fault, all found by looking rather than reasoning: a settings field that never reached the client, and a page whose shelf and rules used different right edges.                                                                                          
Two rounds of HUD and menu work off screenshots: the Discord page redrawn as a page, both match clocks made to tick their last five seconds, and the bounty taken out of the corner.                                                                                                           
Client work 2026-08-23: map names travel on the wire (S2C_MAPNAME 18) and the menu previews every page that shows one; and fx.bend stopped throwing the wreckage it draws, so a ship death expands instead of closing.                                                                         
Nameplate bounties now set as prices with the rivet mark, and the starfield ripple deleted outright: fx.bend was moving wreckage it had no business moving, and the sky effect it bought was not worth keeping.                                                                                
Landing redesign is flush with the rail, drops redundant captions, labels each side k/d, marks AI, puts the hull in a content-following DEPLOY key, and bounds/tests the map. Native WS starts after publish, handles partial/backpressure, caps backlog at 4 MiB. Checks pass.                
Bot fleet hardening: shopping uses actual hulls and completed-flight gating; private flights handle map rotation; map and nav builds use async single-flight weak caching; wire order, decode, and snapshot progress are validated; typed outcomes drive backoff, metrics, and tests.          
User prefers walls in the station's industrial grammar: sparse, varied armor panels, warning marks, pipes, and compact 45/90-degree machinery. Avoid repeated decorations and keep thick-wall patterns fixed as the camera terrain window moves.                                               
2026-08-23 wall rails avoid capsule panels; deep walls need distinct silhouettes. Churn audit removed dead sim/server/client/deploy code, split safe modules, replaced source scans with behavior seams, preserved pack bounds and golden, and passed full validation on main.                 
Walls are plain: keep mass and readable edges, remove all interior decoration, and leave non-wall art intact. Deployment capacity tests must follow fleet.sh's sourced deploy/lib modules; entrypoint-only checks can block both image release and client rollout after refactors.             
Play page went two-column (zones list deploys, readings aside, DEPLOY key only narrow; pages.landing/ground deleted); chrome walls prototyped uncommitted: bands live opaque on alpha vwbg since vwglow only brightens, single-tile faces rim-only, wall_light specular with a glint.          
Chrome v3 uncommitted: face-normal matcap ramp, SKY_OY=-1, hard horizon, grazing side lines, quarter-arc corners; symmetric bands look striped. Ladder: 8 archetypes x 1,024 replicas; wins +1, losses -2 to checkpoint; only powered signed evidence replaces provisional order.              
Two client trims at Chris request: the HUD's INVITE key and session.room_url are gone (corner row is MENU, PLAYERS, room chip), and a diagonal wall's open legs are now drawn, capping a slope run's top and bottom where it meets nothing.                                                    
Assists got their own event and a per-recipient byte on S2C_KILL (protocol 21); Ladder took guests, dropped its play banner, put both ratings on the side name lines, and started logging every finished leg in the same match body.                                                           
Two lessons: git reset --hard after a checkout that aborted on a dirty tree resets the branch you are still on, and melee no longer restarts for the first human, so a deploy joins the match the menu was previewing.                                                                         
Melee scores off the field, so a seat leaving takes its kills: an arrival evicting a bot drops that side's score. The landing is the deck on every window and takes the page: no wash over the fight, no arrival row, a 64pt name, a full-width score bar over a full-width DEPLOY.            
Two client passes: the landing's room band is the live roster with TIME, PLAYERS, SCORE and NEXT MATCH labels, and the whistle stopped drawing wrecks, since a benched hull's death count does not move.                                                                                       
The arena's hull panel with its stat pips is gone (ui.hud lost class_names). The tab row is a full-width bar on the window's top edge with an edge-to-edge rule, the deck reaches to 22pt off the bottom, TIME and PLAYERS wear their own vrules, and the score bar has no label.              
A phone flies with the stick alone now: the d-pad, its ramp, threat.lua and every touch reverse are gone. Ladder's idle first room flies its own duel, a generated-roster stand-in against the rung's rival, and the run resets whenever the climber's seat changes hands.                     
The ship page rebuilt around kept builds, and kill streaks landed in the core.                                                                                                                                                                                                                 
What a streak bonus did to the run a death files, and what it takes to photograph the live client.                                                                                                                                                                                             
Staging a fight needs a small map, and landing on main needed two numbers renumbered around work that got there first.                                                                                                                                                                         
Spectating became one shared feed five seconds behind (follow, the staff grant and the delay dial all gone, protocol 23, decision 59), and a test room ticked past 500 loses its pilots to the lag ladder.                                                                                     
Ship page: profile list is a selector (up/down load builds, NEW under last name, kit stacks from top, RENAME/DELETE dim on starters). Catalog edits need a version bump: mid-deploy arenas pinned unreadable v23 text; take_catalog now refuses catalogs it cannot read.                       
Scoreboard foot line and players-panel foot band removed at Chris request (row marks and the PLAYERS chip carry the counts). Streak ring: seven sparks on unshared breathing frequencies, floored at 0.35 so none goes dark; streak_test pins count and floor.                                 
Slope-pair traps fixed: opposed-face hulls revert to last tick's spot and bounce (golden unchanged). The DEPLOY key breathes on sin(F.now*2.6) like the ON AIR tally, edge floored at 0.62; F.now is 0 under the test harness.                                                                 
The podium is a page not a card: measure 1040, sides abreast over 700 and stacked under it, scoreline unlabeled, SAY and NEXT MATCH over rules, one share key with a tray-and-arrow mark; WATCH REPLAY and BANKED cut. Mocks in .design/podium, artifact 5d417f16.                             
2026-08-24 Spectator-first landing shipped (decision 61): opens as a watcher in melee, wordmark over a breathing PLAY NOW, deck deleted, menu closes over the stands. ui.lua y counts DOWN, state.text UP; the podium washes the window at 0.8, so draw after it.                              
2026-08-25 Nothing opens the menu but a player: with no room yet the client draws the landing minus the room (starfield, MENU, wordmark at its landing spot so it never moves), silent unless a join failed. Close always works; escape means shut from any level.                             
Backticks in a double-quoted git commit -m are command substitution, so use a quoted heredoc into git commit -F -. And mines left the whole game: charge kind, three spec fields, repel conversion, pack's viewer arg, bot mode, client mark, zones, docs. Decision 62.                        
Menu is one column (decision 63): DOCK_W 390 docked left, head with lockup+call sign, DEPLOY over six stops; killed the keyboard picture, shop pane, aside column, MENU_ZOOM, menu.compact. The green CHANNEL mark is gone from the HUD corner too.                                            
The POS tile readout over the radar stays: removed and put back the same night at Chris request, so only the green CHANNEL play mark was meant to go.                                                                                                                                          
The MENU key is a hamburger (bars+word on desktop, bars alone on a phone, box on both, switch is ui.compact) and the menu is a drawer: slides on F.now over 0.16s, a still clock draws it settled, swipe-left drags it off with a third dismissing.                                            
Menu drawer: what it covers stands down by real overlap (clock band, dial corner) via ui.drawer_over. Play page is the games alone: no ZONES head, roster, counts or DEPLOY; x sits left where the burger was, wordmark shifted right, rows lit and pressable full width.                      
Deleting a surface means deleting its leftovers: the drawn keyboard took click_key, its dispatch and the cat/keys fields with it, the roster took its aside branch, and the docs and decision 63 now say what the menu is. Binding is the row asking and the next key answering.               
Ship+upgrades: one hangar merge chosen (.design/hangar, artifact f5cea283, its own branch): circles only (solid=equipped, ring=owned, dim=not yours), reading slides in from right with BUY only when sellable, builds panel behind the name press, points meter shows N left.                 
Hangar v3: ship screen headless (x, name button, points meter on the top row), builds list NEW/DELETE only with NEW sliding to field+CREATE, SAVE a foot key shown only while edited; portrait proves the merged page fits 390x844 with no scroll, SAVE included.                              
Hangar landscape (844x390): drawer docked at 390 beside the fight and radar, the one shape where the page scrolls, SAVE at 32; and the drawer is always semi-opaque (0.86 wash, never a curtain), every board drawing the fight bleeding through, per Chris.                                   
One hangar shipped (decision 64): upgrades tab deleted, every arena slot a circle ladder with prices on the headless ship page, name/dim-circle/enter opens a slide-in reading owning the wallet and BUY, builds behind the band name, SAVE at the foot when edited, 5 stops.                  
Zones carry a label (Melee=Team Battle, Ladder=Duel; keys unchanged); starters are ordinary kit_profiles rows dealt at signup, limit 27. Menu: follow_cursor only when the cursor moves (touch scroll), no wordmark in the drawer head, down reaches the tabs, Save off kit_changed().         
2026-08-25 Standings gone from the client (decision 65): tab, week table, sort/filter, /v1/week fetch, rail mark; tab row is four stops, endpoint stays for the site, glyph and scroll tests re-anchored on friends. The arena's streak line dropped its count: 'is on a streak'.              
The stands delay the whole broadcast, and a press on a game is one act the stands answer.                                                                                                                                                                                                      
Leaving a match is a button on the flying game's row, not a tab stop; and the scoreboard mocks landed in .design/scoreboard: a bare band top center with side stacks aligned to the clock, opening into a players-grammar readout that dims the glass, PLAYERS deleted.                        
Scoreboard band shipped (decision 67): PLAYERS gone, the band is the control, board centered under it, glass washed, ladder banners dropped. Then the match ending was mocked in .design/podium-rethink: four directions by what leads, SAY chips traded for a key hint.                       
Match ending settled (mocks .design/podium-rethink, artifact a8018ad4): no podium, the board comes up at the whistle. Takes-it line, then the bar with TEAM labels and points, the washed pilot list winner-side first, a foot of clock and a small INVITE FRIEND key.                         
The match ending is the board (decision 68): no card, the roster at the whistle winner-side first with MVP and your row washed, a bar head with each side's name inside its share, a foot of clock plus INVITE FRIEND, portrait hugging the foot. Phrase chips gone until a key lands.         
The top HUD row became a row: the band shrank to one key tall (KEY_H 26, clock 36->26 on a monitor) and LINK/POS moved onto the same center via row_mid, with the dial starting at the row's bottom. band_test measures both against the MENU key's own box.                                   
Team Battle measured by a new melee probe: 88 rounds in the air, every bot's 3 repels gone by 18s of 180, and skill barely scores (0.05 vs 0.90 is 0.67 vs 1.45 k/d). Each rung of spray flattens it further: 0.51-1.46 at spray 0, 0.75-1.22 at 3.                                            
Team Battle bombs: nobody buys a rung, the two bombing brains fly Runner kits, and rungs narrow the throw window (near scales with blast, far pinned at 800); ~80:1 gun:bomb in projectiles. Verification: EV_HIT is (victim, attacker), EV_FIRE is once per pull, not per round.              
Five Team Battle fixes landed: multi_delay 100 (the ladder 25/50/75/100 gives 88/78/68/63 rounds in air), repel gate asks what a round costs, bomb far scales off standoff, Halcyon+Ozone to Bomber, per-add-on ceiling clamp. Rounds 88->59, deaths halved.                                   
The five Team Battle fixes are on main at 0238504, catalog v27, both images green. Then the pilot rail stop shipped (decision 69): fifth home stop, helmet mark, the corner call sign a pointer shortcut that never lights for its page; the friends fold was mocked and rejected.             
Pilot page rebuilt (decision 70, artifact 1ef83153): sign up/log in over kept claim mechanics, career from new /v1/career, NEW NAME key, SIGN UP foot key, guest banner and dot once something to lose. Web-container shots: openjdk-25, bob.jar, xvfb, shot.sh; hold mousedown.               
The pilot page arc went live on main (d97a0a1, both images green, /v1/career answering). Then the bot kit stopped being a BuildPlan and is derived from BehaviorProfile: eight personalities, eight kits, correlation up on 5 of 6 maps, k/d ceiling down on all 6.                            
Duel silently ran melee's old spray tuning (the clamp fix made each zone's multi=5 real); a_duel_runs_the_melee_economy now holds the two files. Skill dial swept clean: aim pays only 0.05-0.18, judgment is pure noise, since bearing_err and clean_share clamp off at FLOOR 0.30.           
The skill dial now grades aim, judgment and dodge the whole way up (k/d spans 5x, correlation +0.82 to +0.91); the intermission pass stands the pads down at the whistle and zooms the ending by END.ZOOM 1.45, pinned in podium_test.                                                         
Ending polish landed (no BTY, rivet head, base-size clock/bar, breathing invite; staging a real match needs a pinned client and a local directory). Play menu rethink (.design/play-menu, fa3069ca): three rounds, I spec stacks won; live row figures are the landing room's.                 
Play page format strip shipped and verified live (decision 71, catalog v29): teams/time/scoring derived by ZoneDef::format, drawn as stacks under the hook-line descriptions; CI rode out a GitHub Actions outage and backfilled green. image.yml still has no workflow_dispatch handle.       
Menu rows lit 8 ways; decision 72 made it one (LIT table in ui.lua): wash at drawer span, CURSOR 0.18 / HERE 0.07, hit box = field, wedge gone, standing label breathes 0.74-1. Mocks .design/menu-rows, row_field_test pins it; seeded canvases are git-ignored.                              
Menu polish: one column MENU_PAD 20 from each drawer edge for every page, notes wrapping inside it; and the arrows fixed, up out of the tabs lands on the last row, no slide between root and its page, call sign is a corner stop beside discord.                                             
2026-08-26 Score band on the top row at every size (band_top lost its compact drop; a side drops its name when TOP.chip_right/link_left leave no room). Discord left the game entirely (decision 73): button, page, mark, const and link plumbing gone, invite redirect now on the site.       
2026-08-26 Two subtractions landed the same day: Discord left the game for the site alone (73), and the duel board became pilots, readings and the last 5 named fights, with rung, floor, scoreline and the near-useless MVP mark gone (74).                                                   
The duel board proved live on prod, and the menu keyboard became three walkable rows.                                                                                                                                                                                                          
Two ways to look at what the game does without playing it: a menu page rendered straight to SVG through hud_svg.lua stubs, and the melee probe pricing repels by thirds of the match after danger() replaced the arriving-round test.                                                          
2026-08-26 MVP is the winner's best kills-less-deaths (dec 76), draws and the losing side hand out nothing. Phone toast filter widened mine-only to mine-or-gleam so streak lines show whoever they name; drain_streaks stopped marking mine.                                                  
Ending's INVITE FRIEND key quieted (fill 0.16->0.10, edge 0.95->0.76, breath under PLAY NOW's). Duel draws at the whistle (decision 77): sudden death and its banner gone, a level clock files Drawn and moves no rung; calibration still flies past regulation on purpose.                    
Two surfaces redrawn: weapon marks are the rounds as flown (spray_shape volley, lit bomb, freeze spikes), and the friends page is add field + RECEIVED + dot/name/zone rows with an invite key at the foot (decision 78; sent/here/everybody cut, ignore final).                               
2026-08-26 In-game shots: bake server settings via bob --settings ([vectorwake] server=), fire is SPACE/TAB, local zone 127.0.0.1:9010. Chris's background rethink then shipped as decision 79: ten ideas built behind --config=vectorwake.sky flags, all but the planet.                      
Two landings: the sky (decision 79, world.lua, seeded band and sun on a map name, sky_test pins it) and the badge seat marks (main 41876f4, marks_test rewritten on badge geometry, every check proved by breaking the drawing first).                                                         
2026-08-27 Seat marks became Badges (feathers+chip) after seeing five mocks under the engine. The sky shipped to main as decision 79: map-seeded band/sun/comet/clouds/temp stars/dust, 0.63ms a frame. Client Lua's luacheck CI gate fails on ANY warning.                                    
Team Battle and Duel both play 5 themed rooms (maelstrom/gantry/warren/redoubt/ringworks, catalog v31, gantry calibrates); mapforge v2 gives a theme its own geometry. A wormhole ejects to your own start, so keep warps off the shortest route: no gate catches one.                         
Judge a map by vectorwake-server melee 20 <map>, not mapforge quality: warren's full-height cross walls flattened skill/kd to +0.69 vs +0.87. And the pilot badge's body grew 1.7x at Chris request, floored in marks_test at a quarter of the mark's width.                                   
2026-08-27 Sky draws UNDER the map (vwsky/vwskyglow before vwbg): a sun cannot cull per-tile like a star, and past the map's edge every tile answers solid, so culling on sim.solid also erased the sky outside the room. A vec layer needs :flush() or it uploads nothing.                    
The menu's foot came down onto the rail (decision 80) and the invite became a band like the guest warning; the warning itself was arming a session late, because the career it reads is fetched once a session.                                                                                
2026-08-27 Pilot badge middle is a ship (two quads + engine block, half the mark's width). The sun throws a lens flare: hexagon ghosts on the line through the FRAME's middle, so the chain swings through center as you fly and goes out as the sun leaves.                                   
Pilot badge is the Apex hull outline (3 quads, span 0.44k) with the feathers rooted on its leading edge at each height, not at a shared x. A diamond, an arrowhead+stem and a flat delta all failed; the forked tail is what reads as a craft.                                                 
Badge feather roots follow the hull's leading edge plus 0.075 clearance. Maelstrom overflows both fight layers on terrain alone: 442 rock tiles are 442 per-frame asteroids, 27k fill vs 6144 and 106k glow vs 40960, so hulls and blasts drawn after them are refused.                        
Trimming a rock buys about 2x and maelstrom needs 4.5x, so craters alone leave it over (they do fix shoal). Not animating is the fix: baked static the fight layers go 27k to 120 fill and 106k to 3.8k glow, at the price of bgglow 31k to 140k drawn whole every frame.                      
Rocks are terrain now: draw_rock writes into the static mesh after the stations, the tumble, M.draw_rocks and the craters are all gone. Maelstrom's fight layers fall to 120 fill / 3768 glow and its mesh settles at 5.2 MB. terrain_style_test pins zero discs on a rock.                    
Rocks live on main at e8e6aef (push HEAD:main; a fresh container's local main can be unrelated history). Lehmer is affine, so a cell-grid density test on it lands on a lattice (2x2 all-pass 0.250 vs 0.436); cell_hash squares between two steps for 0.440.                                  
Decision 81: the menu head gap was 3 numbers with a branch, now STAGE_TOP=MENU_PAD off the rule alone plus one section head (pages.SECT_RULE/LABEL); first word 42-61pt under it to 40.2-44. A play row's format strip is pairs, not a map, or the row measures 6pt off.                       
Decision 82: a play row is the game's name and format strip, zone description deleted end to end (catalog v32). Mobile radar cropped not shrunk: RADAR={side=168,crop=2/3} takes one factor off side and SPAN, so a phone gets 112pt reaching 40 tiles at unchanged px-per-tile.               
A room sent S2C_MAP once with try_send and dropped the result, so a full queue left a pilot on the previous map all match; fixed by owing the map. The radar crop went live on 6a162d0: update.sh needs both images for a commit, so a commit superseded mid-CI never deploys.                 
Decision 83 (settings last on every tab row, and a gauge for its mark) came with a crash found by opening the real menu: stage_row took the length of note_lines' nil on a row whose sentence was the empty string, which blanked the whole panel.                                             
Decision 83 and the gauge are live on main e4561c7. Shooting the real client here: bob 1.13 needs JDK 25 plus openal/glu/xdotool/netpbm, and a HELD mousedown opens the menu where a tap and Escape do not. Decision numbers collide, so take the highest at merge time.                       
2026-08-27 Two menu-column jobs: vec.lua gained a scissor so pages no longer slide over the fight (main 3cfae53), and LINK moved from the arena corner into the head beside the account button, standing down where a long call sign would put it over the x.                                  
2026-08-27 Link meter live on main 234302f: bars alone in the menu head. M.pick's near-miss pass is dead in the menu (the column's own pri -1 box is an exact hit), so a small control there needs a real M.TARGET box. Grep the served HTML for the short SHA to prove a deploy.              
Two live changes: the menu's type system landed, and the burst got a five second clock of its own.                                                                                                                                                                                             
Two things the burst work turned up: the snapshot bound has two mirrors, and the melee probe is blind to bursts.                                                                                                                                                                               
Decisions 86 and 87: the radar hugs the top right at PAD 14 like the MENU key, so an upright phone's band drops both side names, and the POS readout is gone with TOP.coord_line and the dial's unread zone. Take the highest decision number at merge time: mine slid 85-86 to 86-87.         
Two controls repriced on 2026-08-27: Chris cut the burst clock to 150 ticks so the rack empties in 3s, and a phone got reverse back as a double-tap stance on the stick, nose held opposite the course.                                                                                        
2026-08-27 Menu pages stopped covering the fight via a vec.lua scissor (main 3cfae53); then Chris flagged the start flow: menu undiscovered, no arena-level spectate, zone press should play rather than re-land on PLAY NOW.                                                                  
2026-08-27 Start flow mocks in .design/start-flow (artifact 16adf113): account/zone/ship stops over PLAY NOW in three directions (Column, Rail, Sentence). Ship stop lists the pilot's saved builds by name, never hulls, SPECTATE last; a zone press means fly.                               
Decision 89 (landing stops) shipped live by merging origin/main and pushing HEAD:main, renumbering at merge; decision 90 holds a duel open 2s after the deciding death, a death inside drawing it, which retired Ladder::on_deaths.                                                            
Decision 88 live at 4f7d65e (a commit whose own image run is cancelled still ships if a descendant gets both images green). Double bomb blast fixed: a prediction-only bomb re-fired a tick off vanished under its old born_key; only rounds an applied snapshot carried detonate now.         
The bomb double-blast fix went live, and the landing's fixed-height column was measured against short windows, where it covers the hull the camera sits on.                                                                                                                                    
The landing's rail shipped as decision 91 and went live on main c039f42.                                                                                                                                                                                                                       
The duel replaced the ladder (decision 92), and filing one taught that a fight must remember its own pair.                                                                                                                                                                                     
Decision 92 live on main c6d56f2, both images green, and the meta restart dropped ladder_progress so banked rungs are gone. Decision 93 then took the suns, comets and lens flare back out of the sky at Chris's ask; band, washes, clouds, stars and dust stay.                               
The landing's stops now light under a pointer (ui.land_hot off ui.pick) and draw call signs, zones and build names raw. Decision 93 is live on main f36211f; grepping the served single-file HTML for prose you edited proves a client change reached the page.                                
The landing became a page both hands work: one cursor the pointer and the arrows share, enter pressing what it names, and names spelled as typed.                                                                                                                                              
Keyboard landing live at cd78a57. Then two duel bugs: a room stopped asking for its bot the tick it arrived, so the director culled it mid-fight at 30s, and a pilot benched waiting for a rival read DESTROYED. The hold says when a bot is sent, not whether it is wanted.                   
The duel fixes are live on main 4ca7a1f (both images green, page stamped 4ca7a1f): a room keeps asking for the bot it has, and a benched pilot reads WAITING FOR A RIVAL. hud_svg.lua has a rival scenario; lua5.1 and luacheck apt-install here.                                              
The duel wait counts down, live on main 2473fef: the hold rides the wire as one byte after the duel status (protocol 29 now), DUEL_HOLD_TICKS moved to modes so the room can count it, and zero reads 'a house pilot is on the way' rather than a resting 0.                                   
Decision 94: the ending has no foot. INVITE FRIEND and the guest's KEEP key are gone (with match_url, keep_pilot, END.share_mark), and the countdown moved into the band, which at the whistle drops its two sides, takes no press, and reads at full strength through the wash.               
The mobile pads were rethought (3 mock directions in .design/mobile-pads) and rebuilt as cluster B in touch.lua: gun key + satellite orbit, rims carry state, multifire off glass, spent keys dimmed not dropped, stick hint drawn by ui.lua.                                                  
The phone's cluster pads went live, then the landing's buttons learned to frost the fight behind them.                                                                                                                                                                                         
The landing's buttons were frosted, and the pads polished: vec.lua arc_aa/ring_aa/round_segs give rings seg's pixel of falloff, gui text nodes gained rot for runs bent round a circle, and only the trigger EV_FIRE named draws a recovery.                                                   
Friends removed end to end (decision 95): tab, page, meta routes, friends+friend_ignores, active_rated_sessions.zone, friends.md, three .design canvases. directory.at_instance stays: it is the deep link's too, and no test covers join/<zone>/<instance>/<room>.                            
Two features came out in one evening: friends (decision 95, live on a689781) and duels (decision 96, taken at merge since both drafts claimed 95). Duels take protocol 30 with them, and the pilot calibration fixture moved to melee's zone.toml.                                             
Dropping arena a2 from compose left it running (no --remove-orphans): still registered, holding melee in the union, its Caddy route gone, so every arena stood down. Three hours dark, and invisible because pick's Stay branch logs nothing.                                                  
The client half of the preconstructed-ships change: the ship page became a roster of whole ships and every page behind it went.                                                                                                                                                                
Balanced the preconstructed roster with calibrate hulls, then found the shipped join hang: a deleted simcore export with Lua callers left behind.                                                                                                                                              
The empty rack and the harness that would have caught it: deal_seat dealt no ammunition, and nothing had ever joined a room and read its own wire back through the client's core.                                                                                                              
Decision 98 live on main 9dd6bc8: play tab and its games page out of the drawer, the landing's zone stop is the one games list, side and leave are tab stops, leave steps one level out. Root cursor must resolve lazily or a late side shifts it onto leave. Only the SHA greps live.         
Decision 99 (mocks .design/pilot-dropdown, artifact 4155b383): the landing's account stop opens a list in place like zone/ship; pilot page, its tab and the call-sign door are gone. Sign up and claim are one act (/v1/claim). Cards draw over the landing via ui.land_card.                  
Decision 99 went live on main f00b902, and decision 100 was built on top: 7 one-credit picks on sim_ship.kit, sim_slot_cap the one ceiling, protocol 32's C2S_KIT carrying spent slots and doubling as the hull change.                                                                        
Decision 100 is live on main 029d1c3, protocol 32, play.vectorwake.net stamped 20:48. calibrate builds set the ceilings (charges 3/2, gun prox+shrapnel+push 0), cutting runaways 88 to 40. CI clippy 1.98 is newer than this box's 0.1.94, so a lint can pass here and fail there.            
No-drawer mocks (.design/no-drawer, canvas 'Landing Settings' ff9cafae): Chris rejected a corner settings key; settings lives in the landing column as a stop at its head (shorter, dimmer, mixer icon) opening a panel like the ship pager. Cockpit key keeps only leave/side/sound.          
Game menu settled (canvas 'Game Menu' 91304fd5, .design/game-menu): settings only in game; the column, no pause (control shifts, ship stays live, thin wash). Opens via Esc + corner MENU key per burger_cap: bars + word on desktop, mark alone on phone. Alternatives kept as record.        
Game menu iterations: side is a list not a stepper (row per side, yours marked, counts along). Menu key: small, faint, bottom middle where the column stands; tap slides the column up, RESUME lands on the key's pixels. Corner row keeps only PLAYERS; Esc unchanged.                        
The hangar's Rung rows never drew because every hull's ladder was one rung, so sim_slot_cap floored the level slot at 0. Decision 101 makes both weapons three-rung ladders: a gun rung is half the row's damage and energy again, a bomb rung +40px blast, and the rate never moves.          
Weapon rungs landed on main b3577c5, a clean fast-forward with decision 101 free at merge. No protocol bump was needed: pack.c already writes all SIM_MAX_RUNGS per trigger, so a client's sim_slot_cap answers 2 straight off the unpacked settings.                                          
/metrics/dir and /metrics/meta on play.vectorwake.net carry vw_build_info commit and uptime, so a deploy is provable without a login; the arena hosts on edge.vectorwake.net are blocked here. calibrate builds is pit-only: every arena mirror bout drew, flattening the table to 50%.        
Decision 102 deleted the drawer: an in-match column (leave/settings/side over RESUME) with a faint MENU key, settings only in game, landing reserving the key strip. Decision 101's rung sweep needed no retune at 40 bouts: gun rung 46.2 is fourth, bomb rung 30.4 tops its band.            
The menu key wears no box, since alone at the foot a box reads as a landing stop or an instrument. And a build sweep on the pit is a fact about the pit: gun bounce is +34.7 over a bare hull there and +7.0 on a real map, so read slots as points over the flight-stat floor.                
Two things that only look settled: 40 bouts a build is a screen and not a test (Wilson +/-14.8 at even, a 10-point gap needs 524, inference lives in experiment.rs), and nameplates come down for anything read over the arena, the landing's panels included.                                 
Two rules the front page got wrong: the Rung row counts from one (base=1 on the level slot alone, land_row adds it) and the guest dot sits beside the call sign; and the menu key is drawn only in a room you are in, escape included.                                                         
Decision 103 (.design/dropdown-stack, canvas ad8a24fb): a stop slides a frosted panel up through the bottom edge and sends the column down through it; head names the stop and carries land_back. Window less 14pt margin, capped PANEL_MAX 560 per Chris. Both columns.                       
Decision 104, one menu language (.design/menu-language, 79274244): menu_row (was stage_row) draws every row, six right ends caret/detail/step/choice/toggle/walk. Panels set F.case sentence; inset 14, height 44/40. Account card is a stacked panel; a covered panel stands down.            
Decision 105, six menu fixes: LIT.field flat (skirt wanted the drawer's rule); the head lights; enter/space toggle a switch; panels are content-height on the foot, easing between heights; LAND_HOT covers every row; sim_splinter_count reads shrapnel as fragments. Mocks match.            
Decisions 106 and 107: a lit menu row is the glass edge to edge (menu_row draws type, pages lay the field via LIT.state, column is ui.ROW_INSET), and the link bars and POS came back over the dial, no LINK word, radar under the top row, bars toggle the debug pane.                        
The debug pane had no opener for four decisions (pages.link died with the drawer at 501bcec). Decision 108: no radar, roster or ending board on the landing (ui.joined = not o.landing); 107's strip splits, bars stay, POS goes with the dial; details/map gate on menu.home.                 
Game strings are not greppable in the served page (the archive is packed): only build: '<short sha>' in the wrapper proves a client change shipped. Decision 107 went live at 31b7f75; a later memory-only commit can supersede yours mid-CI and carry it to the fleet anyway.                 
Decisions 109 and 110 fixed the in-match column: the dead rail's gauge off settings, stops inset 14, sides list at 44, and the whole column drawn at F.text_dim 0.34 because M.hud dims then returns early. ui_harness answering 0 to ship_count hid it from every test.                       
Decision 111 made a column's labels one weight, reversing 109's ink because it was a second fix for the dim 110 had already found; alongside it the ship menu was mocked as five sections in .design/ship-sections (canvas 165ecbef), not implemented.                                         
Ship menu mock, settled: body is a list of 7 hulls each carrying its flight bars, sections read contents in the games-list voice (TYPE.BODY, pal.MUTE), Rung became Level. A content-height panel means the tray stays visible but cannot hold still; phone glass is 362 on a 14 margin.       
Decision 112 built: ship menu is five sections with the tray in panel_frame so it cannot scroll away, body a roster list with per-hull bars, readings in pal.MUTE, Rung now Level. Lua 5.1 has no \u{...} escape; a middle dot is \194\183.                                                    
Body is a carousel drawing the real ship (world.ship's plates, canopy, hardpoints and per-edge brightness) turning on the screen's vertical axis by squashing local x. A panel control at pri 0 is swallowed by panel_hold: test what a press resolves to.                                     
A hull's line on the ship menu is about how it flies, not its shape, and the flight bars came off that menu entirely (116): they live in the body section. note_lines cases once then breaks, so draw wrapped lines raw.                                                                       
Decisions 117-118 reshaped the ship menu: one build belongs to the pilot (L2 gun+bomb, bounce, prox, 1 repel+burst), bots build their own off personality plus seed, and turning the body carousel is now the whole of picking a hull.                                                         
Decisions 119-120 cut the carousel line to five flight bars and spent the default build's seven credits. Then panels learned to open standing on their own head (land_open, menu_cursor), stops lit by value, and the pointer only re-picks once the hand moves.                               
The carousel's box carried the hull it showed, so turning deselected the row, and a dim arrow published no box, so an unaffordable step said nothing. New sfx ui_deny (falling octave 330 to 165, gain 0.42); an arrow lights the row it steps. Live on main 5c2d753.                          
Decision 121 on main 97eb62e: the loadout came off the hull. One gun and one bomb for the room on the original's whole ladder, shrapnel 2-4-8, burst 700; a hull is flight plus footprint. Zone weapons named by rung, [[arena.ships]] flight only, set_ship_class keeps the build.            
Decision 121 is live on the fleet (d30eca5). Decision 122: Misc:BounceFactor is 16, the original's, so a wall gives back all the speed that hit it; friction stays 14/12, a term the original has no setting for, so a wall is lossless head on and not at an angle.                           
calibrate bodies (random body + uniform build a seat, swapped pairs, TOST at 5 points): 99600 seats say the roster is not balanced. Anvil 60% at 4v4 and 69% at 1v1, Cipher 42%; r(win, energy)=+0.89 and r(win, speed)=-0.75, so the bar spread is the roster.                                
Decision 123: recharge runs against the bar (Anvil 1250->875, Cipher 1400/1100->1300/1450, Facet ->1450/1225), taking 4v4 from 11/21 to 19/21 cells inside 5 points. Compressing the energy spread would have been balance by erasure; this pays speed for breaking contact.                   
Alpha Zone's SERVER.CFG obtained (docs said we lacked it): confirms our weapon/energy tables; burst 515 not 700, BounceFactor 22, MultiFireAngle 500. Team Battle took 4 at 791e18e: unlimited ricochet via gun rung bounces=255 (mod_step is shared with bombs), safe_limit 65535.            
Decision 124 live at 42fa718: wormhole is the original's field. Inverse square quoted one tile out (pull 5859 = Alpha Gravity 1500 at 16px), reach 76 tiles set on its own, gravity_bombs on (blast>0 = thrown), ceiling lifted 100 in the field. CFG_VERSION 21, golden regenerated.          
Warp reach halved and live at b249365: wormhole_range 76 -> 38 tiles (baseline.c, zone.toml 608 px). Melee sets no override, so the baseline is what maelstrom and ringworks fly. The replay trace has a wormhole but no hull inside either rim, so the golden held.                           
Decision 125: wormhole_pull 5859 -> 2000, reach 38 and inverse square kept. Point of no return 17-21 tiles -> 10-12. Coincident wormhole tiles sum, so maelstrom's 4-tile core ran 4x and its whole field was inescapable; cut to 2. Stacking costs sqrt(n) inverse square, n at 1/d.          
Decision 125 (wormhole) live at 2b85750. Decision 126: roster held inside Alpha's flight bands via sim_class_clamp (thrust unbounded), rescaled then 3 rows retuned. Team arm 19/21, duel 11/21 unchanged. Slowing the roster made rotation dominant, +11.78 points/100 at high skill.         
Decision 126 live (d954448, CI green, golden reproduced on all three targets). Lag sit-out lands on the channel's warm ring: spectate_silence 500 = CHANNEL_DELAY 500, so a benched pilot's first frame is the tick their inputs stopped; client ignores S2C_LAG state, no swap notice.        
Decision 127 live on main 13b3b1d: S2C_WELCOME carries a trailing why byte (0 none, 1 safe sweep, 2 lag) at protocol 33, and the client says 'moved to spectator: unstable connection'. constant_drift_test now pins CLIENT_PROTOCOL and the WHY codes across Rust and Lua.                    
Defold raises a mouse action for every touch, and on a session's first tap it arrives before the touch with touch.used still false, so the control was pressed twice and a landing stop opened and shut in one frame. pointer_took fixes it; live at 16d62a5.                                  
Playtest harness landed on main 0ba16ed (harness/, docs/architecture/playtest-harness.md): localhost stage, probe.lua, 4 profiles, boot-to-match, oracles. Its lockfile pinned playwright as a link to the container's global copy, so CI's npm ci left a dangling symlink.                    
Body carousel: hull radius 78 -> 62 and the art row 198 -> 166, now derived as 2*(R+PAD)+NAME_H so radius and row cannot drift. Published on main 0e6469a, client CI green end to end. hud_svg's ship fixture predates the carousel and stubs world with no HULLS.                             
Decision 128 supersedes 119: the carousel hull is one ordinary panel row (44) with its name under it at TYPE.ROW in a 26 band, arrows level with the row's middle, hull sentences deleted. Wedge 83x103 points to 27x33. landing_test reads arrow marks off layer.tri.                         
