# Melee: four a side, three minutes, kills.
#
# The room outlives the match. It plays one, puts up a podium for twenty-five
# seconds, changes ground, and plays another, so a pilot who presses Play once
# never touches a menu again until they want to. docs/design/match-game.md is
# the argument for all of it.
#
# The tuning below came across from the open arena this replaced, where it was
# arrived at by playing rather than out of anybody's settings file. What is new
# here is the clock, the two pockets, and the numbers that had to move because
# a match is three minutes rather than an evening.

label = "Team Battle"
# The format strip beside this carries the numbers (teams, clock, scoring),
# so the sentence carries the one rule the numbers cannot: the bounty.
description = "the longer your run, the bigger the bounty on you"

# The clock and the score are the mode's; kills are what it counts.
mode = "melee"

# Six maps, each from the recipe and metrics beside it. The rotation crosses
# four topologies, four visual themes, three arena envelopes, and two arena
# silhouettes before it repeats a concept. `mapforge verify` proves that a map is
# the exact output its recipe names.
maps = [
  "drydock.vwmap",
  "relay.vwmap",
  "convoy.vwmap",
  "shoal.vwmap",
  "breakwater.vwmap",
  "switchyard.vwmap",
]

# Eight seats, all eight of them a person's if eight people want them. The bot
# server holds every empty one and stands down the moment somebody arrives,
# which is what makes "press play and you are in a match" true at this
# population.
max_players = 8
max_ships = 8
bot_fill = 1.0

# A second room opens only when the first holds eight humans. That is the whole
# of the concentration rule, and at this population it means everybody who
# turns up is in the same match.
fill_target = 8

# Ten rooms to a process, which is a ceiling rather than a count: a room
# appears when every live one is full and is reclaimed when it empties.
max_rooms = 10

# Two sides, four each. Named for things that hold a structure up, which is the
# register the hulls are named in, and neither is a name any pilot wears.
teams = ["Pylon", "Caisson"]
# Two, so nobody founds a third side inside a game built for two.
max_teams = 2
max_humans_per_team = 4
max_bots_per_team = 4

[arena]
# Three minutes of play, fifteen seconds of podium. It was twenty-five, which
# is a long time to look at a result on an empty arena: the wait is for reading
# the podium and getting to the hangar, and both fit inside fifteen.
match_seconds = 180
intermission_seconds = 15

# A wall gives back twelve sixteenths of what hit it. There is no separate term
# for speed along a wall, so both take the same number.
bounce = 12
friction = 12

# Two seconds dead. The open arena waited four, and four was fine there because
# the map was a thousand tiles wide and the wait was the smaller half of the
# penalty. Here the walk home is the penalty, described below, and four seconds
# of staring at a card is a fortieth of the match.
respawn_delay = 200

# Exactly on the map's own starts, which is what a pocket is for.
#
# The open arena scattered arrivals over sixty pixels because it put fifty-one
# ships on sixteen points and they landed on top of each other. Here there are
# four points a side and four ships a side, and `sim_restart` walks a side
# along its own starts, so every pilot opens a match on a tile of their own.
# A radius would only blur the one piece of geometry the whole design leans on.
spawn_radius = 0

# Neither map draws a safe zone, and neither should: a safe pocket is where a
# leading side goes to stall out a clock. The limit is set anyway, because a
# zone that later wants one should not also have to remember this line.
safe_limit = 3000

# A pilot is worth one the moment they spawn and one more for every kill on the
# run. Their killer takes it. So the number over a ship is the length of its
# current streak and nothing else, which is a sum a player can do at a glance,
# and a fresh spawn is worth almost nothing to camp. docs/design/bounty.md.
bounty_base = 1
bounty_per_kill = 1
points_per_flag = 0

# Five degrees between rounds. Between tunings this was the one setting that
# moved a lot: a 15-degree fan was worth +5 win points and a 5-degree one +29,
# because a fan nobody can land is not a weapon.
mod_spread = 5

# What one more round costs, as a percentage of the shot's own energy and
# cooldown.
#
# The delay is a hundred because a rung must never buy sustained rate. One
# rung adds one whole base cooldown, so a four-round pull waits four times as
# long and throws the same rounds a minute a bare barrel does. What the points
# buy is the volley: four rounds arriving at once instead of spread over a
# second, in a fan wide enough to cover a dodge, at a quarter less energy each.
#
# It was twenty-five, which made the two costs cancel: a pull at spray three
# charged 1.75 times the energy over 1.75 times the cooldown, so the energy
# drain per second never moved and the rounds per second went up by 2.3. Spray
# was not a trade at all, it was a free upgrade nobody could decline, and the
# room played like it. Measured over forty matches on drydock, moving this from
# twenty-five to a hundred took the rounds in the air from 88 to 63, the deaths
# from 2.0 a pilot-minute to 1.3, and the roster's accuracy from the low
# thirties into the high forties. `vectorwake-server melee` is the harness.
multi_energy = 25
multi_delay = 100

# Three tiles of fuse, and a tile more for each rung of the bomb carrying it. A
# fuse that has found somebody waits `prox_delay` for them to start pulling
# away before it goes off anyway, and a proximity bomb will not leave the tube
# with an enemy already inside the fuse's distance, so it cannot be carried up
# to a hull and posted through it.
prox_step = 16
prox_delay = 150
bomb_safety = true
# A hull whose bombs bounce pays nothing for it.
bbomb_damage = 1000
# Three damage over the first quarter second, which is what stops a bomb landing
# twice at point blank.
shrap_inactive = 3
shrap_inactive_ticks = 25

[arena.mod_step]
# One rung of spray is one more round, and proximity is measured in pixels. A
# rung of bouncing is one more wall on top of what the weapon already had,
# which is one wall for a bomb and nothing worth counting for a bullet.
multi = 1
bounce = 1
prox = 48

# The damage of one of the twenty-four rounds a burst throws. Lower than the
# baseline's 700, so a burst is survivable at a range that one is not.
[[arena.weapons]]
name = "burst"
damage = 515

# The ships.
#
# There is no ships section. Every hull here flies alike, climbs alike and
# holds alike. What tells them apart is how each spends the same target budget:
# every oriented collision rectangle occupies exactly 625 square pixels. A
# Cipher is 16 pixels across its flank and just over 39 down its length, while
# Chord makes the opposite trade. The client art is fitted around those fixed
# extents, and a zone has no business moving them.
#
# This used to be seven blocks: a rotation for the interceptor, a speed for
# the bomber, a recharge for the skirmisher, and a row of add-on ceilings
# apiece. Both are gone, for the same reason. Thirty points has to mean the
# same thing to everybody or a kit is not a fair trade, and an add-on ceiling
# that lives on a hull is an upgrade the shop can never sell. See
# docs/design/ships.md.

# What a kit may hold here, once, for every hull alike. A pilot spends thirty
# points inside these ceilings; the shop sells the right to reach the ones
# their account has not bought yet, never a point more to spend.
#
# These are the baseline's own numbers, written out because this is the zone
# people actually play and the ceilings are worth reading in one place. Guns
# spray, bounce and freeze. Bombs bounce, sense, shatter and freeze. Neither
# crosses over: a bullet with a fuse is a bomb, and a rack that throws three at
# a pull is a different game.
#
# Spray to three, which is four rounds at a pull.
#
# This said five, and five is what the core allows, but the arena clamped every
# add-on at three on its way in and nothing said so: the number here has never
# been the number in the room. That clamp is fixed, so this is now the figure
# it looks like, and three is deliberate rather than inherited. Four rounds
# already fill a lane, and each rung above costs the room in bullets it does
# not pay back in decisions: measured over forty matches, going from three to
# five put another three rounds in the air across the room and narrowed the gap
# between the best pilot on the roster and the worst.
[arena.kit]
gun_mods = { multi = 3, bounce = 1, freeze = 1 }
bomb_mods = { prox = 1, shrapnel = 3, bounce = 2, freeze = 1 }

# Three repels and three bursts for anyone who buys the rungs and is willing
# to spend a fifth of their kit on them.
charges = [3, 3]
